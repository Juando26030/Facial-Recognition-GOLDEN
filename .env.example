DATABASE_URL=postgresql://usuario:password@localhost:5432/golden_db

# development (default) deja /docs, /redoc, /openapi.json abiertos y la cookie de sesión sin flag Secure.
# En producción, poner ENVIRONMENT=production (obligatorio junto con SECRET_KEY).
ENVIRONMENT=development

# Secreto para firmar la cookie de sesión. Generar uno único por entorno, ej:
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=cambia-esto-por-un-valor-aleatorio-largo

# Correo saliente (informe final del evento -> comercial). Sin SMTP_HOST no se envia nada: el correo
# se guarda como .eml en data/outbox/ (util en desarrollo).
# SMTP_HOST=smtp.tu-proveedor.com
# SMTP_PORT=587
# SMTP_USER=notificaciones@tu-dominio.com
# SMTP_PASSWORD=...
# SMTP_FROM=Golden Eventos <notificaciones@tu-dominio.com>
# SMTP_TLS=true

# Escarapela digital: dominio público con el que se arma el enlace (por defecto, el de la petición) y SMS por Twilio.
# PUBLIC_BASE_URL=https://eventos.tu-dominio.com
# TWILIO_ACCOUNT_SID=...
# TWILIO_AUTH_TOKEN=...
# TWILIO_FROM=+15551234567

# Zona horaria de los reportes de Áreas/Inventario/Legalizaciones (por defecto America/Bogota).
# APP_TIMEZONE=America/Bogota
