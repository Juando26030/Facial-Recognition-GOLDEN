# Golden Biometrics — Facial Recognition GOLDEN

## Qué es esto
SaaS de control de acceso e identidad por reconocimiento facial, pensado para reemplazar planillas/tarjetas físicas en sedes de "Golden Logísticas". Backend FastAPI + PostgreSQL, motor biométrico con `face_recognition` (dlib), frontend server-rendered (Jinja2 + JS plano). Pensado para desplegarse en una VM de Google Cloud detrás de Nginx/Cloudflare, con systemd gestionando los procesos.

Repo: https://github.com/Juando26030/Facial-Recognition-GOLDEN

**Documentos de producto:** viven en [`docs/`](docs/00_README.md) (agregados 2026-09-19) — visión, roadmap de requerimientos, clasificación FURPS+, backlog Scrum, arquitectura a nivel producto, modelo de datos. Se generan y mantienen desde OTRA sesión de Claude (dedicada a producto/documentación), no desde la sesión de código — si algo en `docs/` no coincide con lo implementado, manda el código y este `CLAUDE.md`, no `docs/` (`docs/00_README.md` lo dice explícitamente). Este `CLAUDE.md` sigue siendo la única fuente de verdad del detalle técnico/arquitectura real del código.

## Arquitectura
```
app/
  main.py           FastAPI app: SessionMiddleware, /  (dashboard) y /kiosk/{event_id}, incluye routers
  database.py       Engine SQLAlchemy, lee DATABASE_URL desde .env (obligatorio, sin fallback)
  models.py         Tenant, User, AccessLog, StaffUser, Event, EventStaffAuthorization
  auth.py           Hashing (bcrypt), get_current_staff, require_role(minimum), get_event_for_staff
  biometrics.py     BiometricEngine: extracción y comparación de encodings faciales
  reports.py        ReportManager: genera reporte Excel (pandas + openpyxl)
  routers/
    api.py            Endpoints biométricos (recognize/register/users/report) bajo /api, event-scoped
    auth.py            /login, /logout
    tenants.py          CRUD de clientes (tenants) bajo /api/tenants
    events.py          CRUD de eventos (/api/events, /api/my-events, /api/events/search,
                        /api/cities) + cuentas digitador/cliente atadas a un evento
                        (/api/events/{id}/staff-users, .../assign-existing)
    staff.py            Cuentas coordinador/admin (no digitador/cliente, ver Roles y permisos)
    badges.py           Escarapelas: editor de plantilla por evento, librería reusable por
                        tenant, datos/foto para imprimir (ver sección Escarapelas)
    cedula.py           Lectura por foto de la MRZ de la cédula nueva (ver sección Lector de cédula)
  mrz_parser.py     Parseo + validación (checksum ICAO 9303) de las 3 líneas MRZ ya extraídas —
                    puro texto, sin imágenes, se puede probar sin OCR real (ver sección Lector de cédula)
  mrz_ocr.py        OCR (pytesseract) de la foto del reverso de la cédula nueva — necesita el
                    binario de Tesseract instalado en el sistema, ver esa misma sección
  cities_data.py    Carga app/cities_by_country.json (dataset real GeoNames) — ver sección de ciudades
  cities_by_country.json   ~20,300 ciudades reales (GeoNames), generado offline una vez
alembic/            Migraciones versionadas (ver sección Migraciones) — reemplaza Base.metadata.create_all
scripts/create_staff_user.py   Bootstrap del primer Super Admin (o cualquier cuenta) desde la terminal
static/
  js/clockpicker.js   Selector de hora tipo reloj analógico (custom, sin dependencias) — ver abajo
  js/toast.js         showToast()/showConfirm() — reemplazo no-bloqueante de alert()/confirm()
  js/app.js           Lógica del kiosko (escáner, registro, tab switching, etc.)
  js/directory.js     GoldenDirectory.render()/load()/mountSearch() — Directorio en Vivo + búsqueda, usado por la vista de Registro unificada
  js/badge-render.js  BadgeRender.renderBadge()/renderElement() (compartido por editor e impresión, mismo código en los dos lados a propósito) + BadgePrint.openPrintWindow()/maybeAutoPrint() (ver sección Escarapelas)
templates/          dashboard.html (`/`), kiosk_select.html (`/kiosk/{event_id}`),
                    kiosk_registro.html (`/kiosk/{event_id}/registro`, Registro unificado — ver sección abajo),
                    kiosk_roster.html (`/kiosk/{event_id}/roster`),
                    badge_editor.html (`/kiosk/{event_id}/escarapela`, editor visual — ver sección Escarapelas),
                    badge_print.html (`/kiosk/{event_id}/escarapela/imprimir/{user_id}`, vista de impresión),
                    login.html, staff.html
data/<tenant>/badge_assets/   Imágenes de fondo/logo de escarapelas por tenant (gitignored, mismo patrón que known_people/)
data/<tenant>/known_people/   Fotos de registro por tenant (gitignored, no confundir con app/cities_by_country.json — ese NO está en la carpeta "data/")
```

### Navegación (2026-09-15)
`/` ya NO es el escáner — es un dashboard según rol:
- `coordinador`/`admin`/`super_admin`: lista de **Tenants (clientes)**, expandible a sus **Eventos**, con formularios inline para crear cliente/evento. Cada evento tiene un botón "Ingresar" que lleva a `/kiosk/{event_id}`.
- `digitador`: lista plana de sus eventos autorizados y activos (`GET /api/my-events`), mismo botón "Ingresar".

`/kiosk/{event_id}` es la pantalla de escaneo/registro/directorio/reporte, **atada a un evento concreto**: `app/main.py` valida el acceso con `get_event_for_staff` antes de renderizarla (404/403 → redirige a `/`), y le inyecta `window.EVENT_ID` al JS. Todas las llamadas de `static/js/app.js` a `/api/recognize`, `/api/register`, `/api/users*`, `/api/bulk_register`, `/api/report` ahora mandan `event_id` (query param o campo del FormData) — el backend resuelve el `tenant_id` a partir del evento, ya no hay tenant fijo.

**Registro unificado (2026-09-21, reemplaza el picker de "Facial vs Cédula" de abajo) — pedido explícito del usuario tras el primer round de testing manual.** Facial y Cédula eran dos templates/rutas separadas (`kiosk.html`/`kiosk_cedula.html`) que en la práctica compartían el mismo Directorio en Vivo y básicamente toda la sección de gestión — la única diferencia real era si había fotos cargadas o no. Se fusionaron en **una sola pantalla, `templates/kiosk_registro.html`** (`GET /kiosk/{event_id}/registro`), cuyo contenido se adapta según **`Event.facial_enabled`** (`Boolean`, migración `0010_event_facial_enabled`):
- **`facial_enabled=True`:** aparece la pestaña "Escáner de Acceso" (cámara) además de todo lo demás; en "Registro Individual" se pide la fotografía.
- **`facial_enabled=False`:** no hay pestaña de escáner, y "Registro Individual" no pide foto — el alta es exactamente la de cédula tradicional (identidad, sin foto).
- **En AMBOS casos** existe "Directorio en Vivo" con los 3 campos de búsqueda (cédula/nombre/empresa, filtran en vivo sin golpear el backend) + botón "Acreditar" por fila + atajo de lector de código de barras (Enter con cédula exacta → `checkin-cedula` al instante, con el flujo `DUPLICADO`/`force` de siempre) — esto ya NO es exclusivo de "Cédula", es el directorio único de la vista de Registro, tenga o no cámara el evento. Si el lector/búsqueda no encuentra la cédula, en vez de un mini-formulario aparte (como hacía la vieja `kiosk_cedula.html`) se salta a la pestaña "Registro Individual" con la cédula precargada — un solo formulario de alta manual, no dos.
- **¿Quién prende `facial_enabled`?** Se enciende sola (nunca se apaga sola) la primera vez que `POST /api/bulk_register` recibe un `zip_file` real para ese evento — ver sección de carga de base más abajo. Un evento nuevo empieza en `False` (cédula tradicional) hasta que alguien suba fotos.
- `GET /kiosk/{event_id}` (`main.py: kiosk_entry`) sigue sin renderizar directo el escáner. Para `cliente` (que no registra nada) va directo a `kiosk_registro.html` (su vista reducida Estadísticas+Directorio — con búsqueda pero sin Acreditar/atajo, es de solo lectura). Para todos los demás roles, `templates/kiosk_select.html` (picker) ahora solo tiene 2 tarjetas reales: **"Registro"** (→ `/kiosk/{event_id}/registro`) y **"Adjuntar Base de Datos"** (→ `/kiosk/{event_id}/roster`) — **QR** sigue deshabilitada ("Próximamente").
- **Rutas viejas `/kiosk/{event_id}/facial` y `/kiosk/{event_id}/cedula`** quedaron como **redirect 302** a `/kiosk/{event_id}/registro` (`main.py: kiosk_registro_legacy_redirect`) — no se borraron de plano por si alguien tenía el enlace guardado.
- `static/js/directory.js` ganó **`GoldenDirectory.mountSearch(opts)`** — antes la lógica de los 3 campos de búsqueda + atajo de Enter vivía inline y duplicada en `kiosk_cedula.html`; ahora es parte del módulo compartido (`render`/`load` pasaron de ser propiedades de un objeto literal a funciones nombradas en el closure, para que `mountSearch` las pueda llamar directo sin depender de `this`). `opts: { tbodyId, searchIds: {cedula, nombre, empresa}, showAccredit, fastCheckin, onNotFound }`, devuelve `{ reload() }`. `app.js: loadLiveDirectory()` ahora delega a `window.directorySearch.reload()` cuando existe (lo setea `kiosk_registro.html` al montar), en vez de recargar la tabla "a secas" y perder los filtros ya escritos.
- Las pestañas de gestión (Exportar, Usuarios del Evento) siguen ocultas para `digitador` (Exportar) o exigiendo `coordinador`+ (Usuarios del Evento), igual que antes — nada cambió ahí, solo viven en el template unificado en vez de uno separado por método.

**Registro Individual con campos opcionales dinámicos (2026-09-22) — pedido explícito del usuario: "los mismos campos que tenemos en el Excel, con los opcionales que yo pueda añadir... hasta 30... y que me pida aclarar qué sería cada opcional".** El formulario de alta manual (`#regForm` en `kiosk_registro.html`) ya no se queda corto frente al roster — ahora tiene:
- **"Tipo de Asistente"** (`opt_1`) — el mismo campo fijo que ya existía en el roster, faltaba en el alta manual.
- **Campos opcionales `opcional_N`**, renderizados según `Event.optional_field_labels` (los que ese evento ya tiene rotulados, sea porque se subió un Excel con esas columnas o porque se agregaron aquí mismo antes) — `main.py: _resolve_kiosk_page` calcula `optional_labels` (lista `(clave, rótulo)` ordenada numéricamente, no alfabéticamente — "opcional_2" antes que "opcional_10") y se la pasa al template.
- **Botón "+ Agregar campo opcional"**: agrega un campo nuevo al vuelo (hasta completar los 30), reutilizando el mismo modal de `window.promptOptionalLabels()` (movido de `kiosk_roster.html` a `toast.js` para compartirlo) para preguntar de una vez a qué corresponde — el nombre elegido viaja con el registro (`field_labels`) y se guarda en `Event.optional_field_labels`, disponible desde ese momento para todos (reportes futuros, próximas altas, próximas cargas de Excel). **Funciona igual para un evento sin ningún Excel** (todos los campos se agregan a mano desde cero) que para uno con roster ya cargado (los campos existentes aparecen automáticamente con su nombre real).
- Backend: `manual_register` ahora acepta `extra_fields` (JSON) y `field_labels` (JSON), y sigue el MISMO patrón `NEEDS_LABELS` que `bulk_register` (factorizado en el helper compartido `_apply_optional_labels()`) — aunque en la práctica casi nunca se ejerce porque el botón "+ Agregar" ya preguntó el nombre en el momento. `_parse_extra_fields()` es el helper compartido para leer y limpiar ese JSON.
- Frontend: `app.js`'s `regForm.onsubmit` recolecta todos los `<input name="opcional_N">` sueltos del formulario en un solo `extra_fields` JSON antes de enviar (el backend espera un JSON, no campos sueltos) y agrega `field_labels` con lo que `window.getPendingOptionalLabels()` tenga acumulado en esta carga del formulario (se limpia con `window.clearPendingOptionalLabels()` tras un alta exitosa).

**Bloqueo de re-carga de base sobre un evento ya EN PROCESO (2026-09-22) — pedido explícito del usuario: evitar que alguien resuba la base por error y pise registros que ya se hicieron en vivo.** Dos capas:
1. **Aviso siempre, en el frontend** (`kiosk_roster.html`, antes de mandar la petición): si `event.status == "en_proceso"`, `showConfirm` pastel amarillo preguntando si de verdad quiere cargar la base — se muestra tanto en la primera carga durante el evento como en cualquier reintento, es solo una pausa para pensarlo, no bloquea.
2. **Bloqueo real, en el backend, solo para el caso de RE-carga.** `Event.roster_uploaded` (migración `0011_roster_uploaded_flag`, `Boolean`) se enciende sola la primera vez que `bulk_register` procesa al menos una fila con éxito (`count > 0`), sin importar en qué estado estaba el evento en ese momento. Si luego `bulk_register` se vuelve a llamar con el evento en `en_proceso` Y `roster_uploaded` ya en `True`, responde **400 sin excepción ni `force`** — "no te deje", literalmente, la única salida es crear un evento nuevo. **A propósito NO bloquea** una recarga legítima mientras el evento sigue en `creado` (antes de empezar, es preparación normal) ni la primera carga durante un evento que arrancó sin base (facial/cédula 100% manual desde el principio) — el bloqueo es específicamente "ya había una base Y el evento ya está corriendo Y quieren mandar otra".

<details>
<summary>Histórico: cómo era antes de la unificación (2026-09-17 a 2026-09-20) — se deja para contexto, ya no refleja el código actual</summary>

Facial y Cédula eran dos "métodos" elegidos desde una pantalla `kiosk_select.html` con tarjetas, cada uno con su propio template (`kiosk.html`/`kiosk_cedula.html`) y su propia ruta (`/facial`, `/cedula`), pensado para que **varias personas operaran el mismo evento en paralelo con métodos distintos** (uno con cámara, otro con lector de código de barras). Cédula se rediseñó en 2026-09-20 para parecerse al Directorio en Vivo (búsqueda + Acreditar) en vez de un único input minimalista. La carga de base (antes pestaña "Carga Masiva" dentro de Facial) se sacó a su propia tarjeta "Adjuntar Base de Datos" el mismo día. Esta arquitectura de "métodos separados" quedó reemplazada por el Registro unificado de arriba porque, en la práctica, la diferencia entre los dos templates era solo "¿hay fotos o no?" — algo que ahora vive como un flag en el `Event`, no como dos pantallas distintas.
</details>

**Carga de base de asistentes, agnóstica al método (2026-09-19, Épico 1/Historia 1.1 — `docs/03_PRODUCT_BACKLOG.md`):** `POST /api/bulk_register` (mismo endpoint de siempre, comportamiento extendido) acepta `roster_file` en **`.xlsx` o `.csv`** (antes solo CSV — el negocio manda Excel, ver `docs/00_REQUERIMIENTOS_ROADMAP.md` §2.1) vía `_read_roster_rows()`, que normaliza ambos formatos a las mismas filas antes de procesarlas. `zip_file` sigue siendo opcional: sin zip solo crea/actualiza identidad (`User`) y asocia a `EventAttendee` (suficiente para acreditar por cédula); con zip además procesa fotos a encoding biométrico (necesario para Facial). No se permite cargar sobre un evento `finalizado` (sí sobre `creado`/`en_proceso`, es preparación previa). Los errores de fila no abortan la carga completa — se acumulan en `errors: []` en la respuesta. **Plantilla descargable (2026-09-20, regenerada el mismo día para el esquema de campos dinámicos):** `static/templates/plantilla_carga_base_asistentes.xlsx` (2 hojas: "Base de Asistentes" con los headers exactos que `_read_roster_rows` espera + fila de ejemplo, e "Instrucciones") — enlazada desde `kiosk_roster.html` ("Descarga la plantilla de Excel"). Si se agregan/renombran columnas reconocidas por `bulk_register`, regenerar este archivo para que siga reflejando el formato real.

**Campos opcionales dinámicos, hasta 30 (2026-09-20, reemplaza el esquema rígido de `opt_1`/`opt_2`):** el negocio puede necesitar columnas distintas según el evento (talla de camisa, grupo, restricción alimentaria, número de mesa...), así que en vez de dos campos fijos con nombre fijo, el roster ahora acepta columnas **`opcional_1` a `opcional_30`** (`MAX_OPTIONAL_FIELDS` en `routers/api.py`), reconocidas con o sin guion bajo/espacio ("Opcional 1", "opcional_1", "opcional1" → todas normalizan a `opcional_1` vía `_normalize_optional_key()`). Solo se procesan las columnas que el archivo realmente trae **con al menos un valor no vacío en alguna fila** — las demás (de las 30 posibles) se ignoran, no hace falta que la plantilla las traga todas llenas.
- **`tipo de asistente`** (antes "tipo de empresa") sigue siendo el único campo opcional de nombre fijo — sigue viviendo en `User.opt_1`, ahora con ese rótulo en toda la UI (formularios, tabla de Directorio). `User.opt_2` (antes "cantidad de empl") queda **deprecado**: la columna sigue en la base (no se botó en la migración, para no perder histórico) pero ya no se escribe ni se muestra en ningún lado — su rol lo absorben los 30 opcionales.
- **`User.extra_fields`** (migración `0009_optional_fields`, columna `Text`, JSON manual vía `get_extras()`/`set_extras()`, mismo patrón que `face_encoding`) guarda `{"opcional_1": "valor", ...}` por persona — solo las claves que ese roster trajo con dato.
- **`Event.optional_field_labels`** (misma migración, mismo patrón JSON) guarda **por evento** qué significa cada `opcional_N` que ESE evento usa — ej. `{"opcional_1": "Talla de camisa"}`. Es por evento y no global porque un opcional_1 de un evento puede no tener nada que ver con el de otro.
- **Flujo de "pregúntame a qué corresponde" (mismo patrón `force` de la advertencia de doble registro, ver abajo):** `POST /api/bulk_register` detecta, ANTES de tocar la base o el zip, qué `opcional_N` trae el archivo que el evento todavía no tiene rotulados. Si hay alguno, devuelve `{"result": "NEEDS_LABELS", "fields": ["opcional_1", ...]}` sin commitear nada. El frontend (`kiosk_roster.html: promptOptionalLabels()`) muestra un modal pidiendo un nombre por cada campo detectado, y reenvía LA MISMA petición (mismo archivo, mismo FormData) agregando `field_labels` (JSON) — con eso el backend guarda las etiquetas en `Event.optional_field_labels` y sigue con la carga normal. Una vez rotulado, las siguientes cargas de ESE evento con las mismas columnas ya no vuelven a preguntar.
- **Pendiente / backlog:** ni el Directorio en Vivo ni el Reporte Excel (`reports.py`, formato legacy fijo que no se tocó) muestran todavía los `opcional_N` con su label — por ahora solo quedan guardados en `extra_fields`. Vale la pena exponerlos cuando se decida cómo mostrar un número variable de columnas por evento sin romper el formato legacy del reporte.
- **Bug real encontrado y arreglado (2026-09-20): `bulk_register` con dos filas de la MISMA cédula en el mismo archivo tumbaba TODA la carga con `IntegrityError` (`ForeignKeyViolation` en `event_attendees`), 500 al usuario, nada guardado.** Causa: la sesión de SQLAlchemy usa `autoflush=False` (`database.py`) y el loop de filas hacía un solo `db.commit()` al final de TODO el roster. Sin autoflush, la segunda fila con la misma cédula no veía el `User` recién creado por la primera fila (todavía no se había mandado a Postgres), así que la tabla `event_attendees` terminaba con una fila referenciando un `User` que en la práctica nunca se insertó como se esperaba dentro del mismo flush masivo — reproducido y confirmado con un test local (SQLite en memoria) antes y después del fix. Arreglo: cada fila corre dentro de su propio SAVEPOINT (`with db.begin_nested():`) con `db.flush()` explícito después de crear/actualizar el `User` y de nuevo después de `_upsert_attendee` — si una fila falla, se revierte SOLO esa fila (la transacción de afuera sigue sana) y se reporta en `errors[]`, en vez de arrastrar a todo el archivo. Esto también resuelve, de paso, cédulas duplicadas dentro del mismo archivo (ya no intenta crear el mismo `User` dos veces). **Ojo con este mismo patrón (`autoflush=False` + un solo commit al final de un loop) si se agrega otro endpoint que inserte en batch — replicar el SAVEPOINT-por-fila ahí también.**

**Validación con referencia de celda exacta (2026-09-20) — para que el error de arriba (y cualquier otro problema de datos) se vea claro para quien sube el Excel, no como un stack trace.** `_read_roster_rows()` ahora devuelve `(header_columns, rows)`: además de las filas, un mapeo `{"nombres": "B", "correo": "D", ...}` de en qué columna vino cada encabezado — funciona igual para `.xlsx` que para `.csv` (`openpyxl.utils.get_column_letter` sobre la posición, aunque el CSV no tenga "celdas" reales) para poder señalar "B7" sin importar el formato de origen.
- `_field_lookup(clean_row, header_columns, row_num, *keys)` — busca el primer valor no vacío entre varios encabezados alternativos (ej. `'nombres'` o `'nombre'`) y devuelve `(valor, celda)`; si el archivo trae la columna pero vino vacía, igual devuelve la celda (para poder decir "falta tal dato, revisa B7").
- `_extract_identificador()` — además de sacar la cédula, detecta y corrige automáticamente el clásico problema de Excel de convertir una columna de cédulas en número (`"1020304050.0"` en vez de `"1020304050"`) y lo avisa con una nota `ℹ️`.
- `bulk_register` ahora hace un **pre-escaneo** de todas las filas antes de tocar la base: arma `row_infos` (identificador + celda + notas por fila) y detecta cédulas repetidas en el archivo con TODAS las filas/celdas donde aparecen (`⚠️ La cédula 'X' aparece repetida en 2 filas (A2, A4)...`). Luego, por fila, valida (sin bloquear — la fila se guarda igual, solo avisa): nombre vacío, apellido vacío, correo con formato raro (`_EMAIL_RE`), teléfono con caracteres raros (`_PHONE_RE`) — cada aviso trae su celda exacta.
- Cada mensaje en `errors[]` viene prefijado `❌` (fila omitida por completo, sin ID/cédula), `⚠️` (se guardó igual, pero revisa el dato) o `ℹ️` (corrección automática) — `kiosk_roster.html: renderRosterResult()` los separa en 3 cajas de color (rojo/amarillo/azul pastel) según el prefijo, en vez de una sola lista plana.

**Advertencia de doble registro (2026-09-20) — mismo criterio en los 3 métodos que dejan entrar gente (`recognize`, `checkin-cedula`, `register`):** antes, escanear/acreditar dos veces a la misma persona en el mismo evento creaba silenciosamente un segundo `AccessLog` sin avisar nada — riesgo real de reclamos duplicados (ej. alguien reclamando un obsequio/entrada dos veces) si fue sin querer, aunque también hay casos legítimos (se corrigió un dato y hay que volver a pasar a la persona). Se resolvió con un flag `force`, no con un bloqueo duro:
- `_already_checked_in(db, event_id, user_id)` en `routers/api.py` — true si ya existe un `AccessLog` de esa persona para ESE evento (cualquier método).
- `recognize`, `checkin_cedula` y `manual_register` reciben `force: bool = Form(False)`. Si la persona ya tiene log en este evento y `force` es falso, devuelven `{"result": "DUPLICADO", "details": ..., "data": {...}}` **sin crear el log ni hacer commit** — no basta con no-op silencioso, el frontend necesita el `data` para mostrar el nombre en la advertencia. Con `force=true` (el operador ya confirmó), procede normal.
- **`manual_register` cambió de comportamiento de paso** (antes bloqueaba con `{"error": "..."}` si el `id` ya existía como `User` en el tenant, sin importar el evento — impedía reutilizar a alguien de un evento anterior). Ahora: si el `User` ya existe pero no tiene log en ESTE evento, se le crea el log "Existente" y se asocia sin pedir nada (reusar a alguien de otro evento es válido); si ya tiene log en este evento, aplica el mismo flujo `DUPLICADO`/`force` que los otros dos métodos.
- Frontend: `static/js/toast.js` gana `showConfirm(msg, {variant: "warning", confirmLabel})` (modal pastel amarillo, para advertir sin ser necesariamente destructivo — el `showConfirm` de siempre, blanco+rojo, sigue igual para las confirmaciones de borrar/sobrescribir) y `confirmDuplicateRegistration(data)` (mensaje estándar reusado en los tres flujos). `app.js` (`submitRecognize`, `regForm`) y `directory.js` (`mountSearch`'s `fastCheckin`, botón Acreditar — antes vivía en `kiosk_cedula.html`, ver "Registro unificado" arriba) detectan `result === "DUPLICADO"`, muestran el modal, y si el operador confirma reintentan la MISMA petición agregando `formData.set('force', 'true')` en vez de duplicar lógica.

## Sprint 2 (2026-09-15) — reverificación de Historias 1.3/1.4 antes de cerrarlas

Antes de arrancar Épico 2 (escarapelas), el brief de Sprint 2 (`07_BRIEF_SPRINT_2.md`) exigió reverificar Historia 1.3 (búsqueda) e Historia 1.4 (Directorio) contra `docs/03_PRODUCT_BACKLOG.md` — encontró 2 brechas reales, las 2 corregidas:

**Fix 1 — Búsqueda insensible a tildes/ñ + "prefijo por palabra" (no substring en cualquier posición).** Antes la búsqueda ya era insensible a mayúsculas pero NO a tildes ("maria" no encontraba "María"), y usaba substring puro (buscar "ma" encontraba tanto "María" como "Amaya", lo cual confunde). Se corrigió con el MISMO criterio en los dos lugares que hacen este tipo de búsqueda, a propósito:
- **Backend** (`routers/events.py`, usado por `GET /api/events/search` — la única caja de búsqueda del dashboard, busca a la vez por evento/código/país/ciudad Y por nombre del cliente/tenant, no hay un endpoint de tenants separado): `_strip_accents()` (NFD + descarta categoría Unicode `Mn`, ej. "María"→"Maria", "Compañía"→"Compania") se aplica dentro de `_words()`, antes de partir el texto en palabras — `_matches_by_word_prefix()` (ya existía, ya hacía "cada palabra del query debe ser prefijo de alguna palabra del haystack") no cambió, solo lo que le llega ya viene sin tildes.
- **Frontend** (`static/js/directory.js`, usado por `GoldenDirectory.mountSearch()` — el Directorio en Vivo de la vista de Registro): mismo algoritmo replicado en JS puro, `stripAccents()` (`String.normalize('NFD')` + regex que descarta el rango Unicode de marcas diacríticas `̀-ͯ`) + `wordsOf()` + `matchesWordPrefix()`. Se aplica a los campos de **nombre y empresa** (no a cédula — ese campo se deja como substring/`includes()` de siempre, buscar por los últimos dígitos de una cédula sigue siendo válido y el brief no pidió cambiarlo, solo dio ejemplos de nombre/empresa).
- Verificado con los casos exactos del brief en ambos lados (Python y Node): `"maria jose"`/`"MARIA JOSE"` encuentran "María José Ñúñez Gómez", `"perez"` encuentra "Andrés Pérez López", `"compania"` encuentra "Compañía Ñañez", y `"Ma"` encuentra "María" pero NO "Amaya".

**Fix 2 — Contador/botón "N sin registrar" en el Directorio en Vivo (no existía).** `GoldenDirectory.mountSearch()` ahora crea solo un botón (insertado justo antes de la tabla, no hay que tocar cada template) que muestra cuántas filas están en estado `"No registrado"` — ese estado ya se calculaba bien desde antes (`routers/api.py: get_all_users` — alguien precargado por roster que todavía no se presentó, distinto de `"Nuevo"` que es alguien que se registró por primera vez ahí mismo; se confirmó esto ANTES de construir el contador, tal como pedía el brief). Clic en el botón activa/desactiva un filtro adicional que se COMBINA con los 3 campos de búsqueda existentes (no los reemplaza) — el botón se oculta solo si el conteo es 0 y no está activo el filtro.

## Sprint 2 (2026-09-15) — Escarapelas (Épico 2, Historias 2.1/2.2)

Segunda mitad del brief de Sprint 2, tras las 2 reverificaciones de arriba. Pedido explícito de Juan David: un editor visual de escarapelas mucho más flexible que "fondo + campos de texto" (fuentes reales, colores, imágenes, QR, código de barras, foto del asistente), con una plantilla propia POR EVENTO pero una librería reusable POR TENANT, pensado para imprimirse en una impresora térmica de etiquetas **Brother QL-800** — y la impresión nunca es automática por defecto, el digitador decide.

### Modelo de datos (migración `0012_badge_templates`)
- **`BadgeTemplate`** — la plantilla ACTIVA de un evento. `event_id` es **único y obligatorio** (relación 1:1 con `Event`, no hay "varias plantillas por evento"). `width_mm`/`height_mm` (default 62×100, formato QL-800 vertical), `orientation` (`vertical`/`horizontal`), `background_type` (`color`/`image`) + `background_value`, `elements_json` (lista ordenada por `z_index`, ver abajo), `imported_from_saved_template_id` (FK a `SavedBadgeTemplate`, nullable — **solo trazabilidad, no vínculo vivo**: si se borra la plantilla guardada de origen, este campo se pone en `NULL` y la plantilla del evento sigue intacta, ver el bug encontrado abajo).
- **`SavedBadgeTemplate`** — la librería reusable, a nivel de **tenant** (no de evento): un diseño que gustó se puede guardar aquí y luego importarse como punto de partida en OTRO evento del mismo cliente. Importar **copia** el `elements_json`/tamaño/fondo — editar uno después no afecta al otro, a propósito (si fuera un vínculo vivo, cambiar la plantilla guardada rompería escarapelas ya impresas de otros eventos).
- Ambas usan el mismo patrón `elements_json` (Text/JSON manual, `get_elements()`/`set_elements()`) que el resto del proyecto (`User.extra_fields`, `Event.optional_field_labels`). Cada elemento tiene `{id, type, x, y, width, height, rotation, z_index}` (todo en **mm reales**, no píxeles) más campos según `type`:
  - `text_static` — `content`, `font_family`, `font_size`, `font_color`, `font_weight`, `align`.
  - `text_variable` — igual que arriba + `variable` (un campo real de `User` o una clave `opcional_N` de `Event.optional_field_labels`).
  - `image_static` — `storage_path` (ej. el logo del cliente).
  - `image_variable` — `source: "photo"`; **solo se puede agregar si `Event.facial_enabled=True`** (no tiene sentido pedir una foto que el evento nunca capturó).
  - `qr` — `variables: [lista]` (se concatenan con `|`), `size`.
  - `barcode` — `variable`, `format` (`code128`/`code39`), `size`.
- `Event.auto_print_badge` (`Boolean`, default `False`, misma migración) — el switch de auto-impresión, ver Historia 2.2 abajo.

### Backend (`app/routers/badges.py`)
Mismo mínimo de rol que "Adjuntar Base de Datos" (`coordinador`+) para TODO lo que edita el diseño: `GET/PUT /api/events/{id}/badge-template` (el `GET` crea sola la plantilla con 3 campos por defecto — nombre, apellido, empresa — la primera vez que se abre un evento sin escarapela todavía, así el editor nunca arranca vacío), `GET /api/events/{id}/saved-badge-templates` (filtrado por `tenant_id`), `POST .../badge-template/save-as`, `POST .../badge-template/import/{saved_id}` (scoped al mismo tenant), `DELETE /api/saved-badge-templates/{saved_id}`, `POST .../badge-template/upload-image` (valida extensión, guarda como `<uuid4>.<ext>` en `data/<tenant>/badge_assets/`, mismo patrón que las fotos biométricas). Dos endpoints con mínimo **`digitador`+** (los necesita quien opera el registro, no solo quien diseña): `GET /api/users/{id}/badge-print-data` (todos los campos + `extra_fields` + si tiene foto) y `GET /api/users/{id}/photo` (la foto real). Las imágenes de escarapela se sirven autenticadas vía `GET /api/badge-assets/{tenant_id}/{filename}` (no por `/static`, mismo criterio de "el login es la barrera real" del resto del proyecto).

**Bug real encontrado y arreglado antes de commitear (verificado con `TestClient` + SQLite `PRAGMA foreign_keys=ON`, mismo método que atrapó los bugs de `autoflush` documentados arriba):** `DELETE /api/saved-badge-templates/{id}` tumbaba con `ForeignKeyViolation` si algún `BadgeTemplate` la había importado antes (quedaba referenciada por `imported_from_saved_template_id`). Arreglo: poner ese campo en `NULL` en todos los `BadgeTemplate` que la referencien, justo antes del `DELETE` — coherente con que ese campo es "solo trazabilidad", tolera quedar obsoleto.

### Editor visual (`templates/badge_editor.html`, `/kiosk/{event_id}/escarapela`)
- **Canvas a escala real:** se renderiza a `EDITOR_SCALE=5` — una copia escalada de la plantilla (mm, tamaño de fuente en pt, todo se multiplica por el mismo factor, válido porque ambas son unidades físicas absolutas) para tener un lienzo cómodo de editar, pero lo que se GUARDA sigue siendo en mm reales. Arrastre/redimensión convierten píxeles de pantalla a mm reales dividiendo por el `width_mm` SIN escalar — funciona sin importar el `EDITOR_SCALE` elegido.
- **Mismo módulo de renderizado que la impresión real** (`static/js/badge-render.js`, `BadgeRender.renderBadge`) — a propósito: el editor nunca "miente" sobre cómo va a quedar impreso, solo le agrega arrastre/selección encima.
- **Orientación explícita horizontal/vertical al crear**, con la aclaración de que **vertical es el formato pensado para la Brother QL-800** — determina el canvas y (en `badge_print.html`) el `@page { size: <mm> }` real, no una hoja carta por defecto.
- Catálogo de ~40 fuentes de Google Fonts (cargadas vía `<link>` en el editor Y en la vista de impresión, para que el tipo de letra se vea igual en ambos lados), color/tamaño/peso/alineación por campo de texto.
- Selector de variable para `text_variable`/`qr`/`barcode` poblado con los campos reales de `User` + los opcionales que ESE evento ya tiene rotulados (`window.OPTIONAL_VARIABLES`, inyectado desde `optional_labels_json` — mismo patrón de "serializar a mano con `json.dumps().replace('</','<\\/')`" que ya usaban los demás kiosks, porque Starlette no trae el filtro `tojson` de Jinja2/Flask).
- `image_variable` (foto) bloqueado en el botón "+ Agregar" si `!window.EVENT_FACIAL_ENABLED`, con un toast explicando por qué.
- QR real vía `qrcode-generator` (CDN `cdnjs`, global `qrcode(...)`) y código de barras real vía `JsBarcode` (CDN `cdnjs`) — ambas cargadas en `badge_editor.html` y `badge_print.html`; `badge-render.js` cae a un placeholder de texto si por algún motivo no cargaron, para no romper el resto del editor.
- Guardar (`PUT`), "Guardar como" en la librería (`POST save-as`), Importar desde la librería con confirmación (`POST import/{id}`, reemplaza el diseño actual del evento), switch "Auto impresión" (visible `coordinador`+, PATCHea `Event.auto_print_badge` vía el `PATCH /api/events/{id}` genérico que ya existía).

### Impresión (`templates/badge_print.html`, `/kiosk/{event_id}/escarapela/imprimir/{user_id}`) — Historia 2.2
**La impresión NO es automática por defecto** — pedido explícito del brief. Tras CUALQUIER registro exitoso (escáner facial, alta manual, "Acreditar" del Directorio, atajo de lector de cédula — los 4 puntos donde antes ya se manejaba el flujo `DUPLICADO`/`force`) se ofrece un botón **"Imprimir Escarapela"** (o, en el Directorio, el botón 🖨️ persistente por fila que además sirve para reimprimir en cualquier momento, no solo justo después de registrar) — el digitador decide si lo pulsa. Si el evento tiene `Event.auto_print_badge=True` (switch del editor), la ventana de impresión se abre SOLA además de dejar el botón disponible — nunca al revés (nunca se apaga el botón manual).
- `static/js/badge-render.js`'s `BadgePrint.openPrintWindow(userId)`/`maybeAutoPrint(userId)` son el punto único compartido por los 4 disparadores — abre `/kiosk/{event_id}/escarapela/imprimir/{user_id}` en una ventana aparte (`window.open(..., 'noopener')`) para no sacar al digitador de la pantalla de Registro.
- `badge_print.html` pide en paralelo la plantilla (`GET .../badge-template`) y los datos reales de la persona (`GET .../badge-print-data`), inyecta un `<style>@page{size: <width_mm>mm <height_mm>mm; margin:0}</style>` dinámico con el tamaño REAL de la plantilla (pensado para la Brother QL-800, no la hoja carta por defecto del navegador), espera a que las imágenes (logo/foto) terminen de cargar (con timeout de 2.5s por si alguna falla) y recién ahí llama a `window.print()` — enfoque deliberadamente simple (HTML/canvas + impresión nativa del navegador) en vez de un motor de PDF pesado, suficiente para Chrome/Edge.
- Mínimo de rol para disparar/ver la impresión: `digitador`+ (lo necesita quien opera el registro) — distinto del editor de diseño, que es `coordinador`+.

### QA local (2026-09-15) — 4 bugs reales encontrados con clics/drag reales, los 4 corregidos
Antes de cerrar el épico, Juan David probó Parte 1 en local (`http://localhost:5000/`) con interacción real (no solo lectura de código) y encontró 4 problemas:
1. **[Alto] Código de barras no se renderizaba.** El `<script>` de `JsBarcode` apuntaba a `https://cdnjs.cloudflare.com/ajax/libs/JsBarcode/3.11.5/...` — ese slug (mayúsculas) y esa versión no existen en cdnjs (404 confirmado); el nombre real es en minúsculas, `jsbarcode`, y la última versión publicada es la `3.12.3`. Arreglado en `badge_editor.html` y `badge_print.html`.
2. **[Alto] "Guardar como plantilla" usaba `prompt()` nativo** (`saveAsTemplate()` en `badge_editor.html`), que el navegador rechaza (`prompt() is not supported`) — rompe la convención del proyecto de no usar diálogos nativos (`toast.js`). Arreglo: se agregó `window.showPrompt(message, opts)` a `toast.js` (mismo patrón visual que `showConfirm`, pero con un `<input>` de texto, devuelve `Promise<string|null>`) y `saveAsTemplate()` ahora lo usa.
3. **[Alto] Un `digitador` no podía imprimir** — `GET /api/events/{id}/badge-template` exigía `coordinador`+, pero `badge_print.html` depende de ese mismo GET para cargar la plantilla antes de imprimir, y `digitador` es justo el rol que más imprime el día del evento. Arreglo en `app/routers/badges.py`: se separó el gate — **lectura** (este `GET`) ahora es `digitador`+ (cualquier staff autorizado al evento), **escritura** (`PUT`, editar el diseño) sigue siendo `coordinador`+ sin cambios.
4. **[Alto] `bulk_register` descartaba en silencio una foto sin rostro detectable (o directamente ilegible) del zip**, sin avisarle a nadie — la persona quedaba creada igual, pero sin foto biométrica funcional y sin ningún indicio de por qué. `/api/register` (alta individual) ya validaba esto (`extract_encoding` + rechazo claro) pero nunca se había replicado en el bloque de extracción del zip de `bulk_register` (antes: `try: ... except: pass` mudo). Arreglo en `app/routers/api.py`: cada imagen del zip ahora pasa por la MISMA validación de rostro antes de guardarse — si no decodifica, o si decodifica pero no tiene rostro, NO se guarda el archivo y se agrega un `⚠️` a `errors[]` (mismo array/convención de prefijos que ya usa el resto de la carga, sin inventar un campo nuevo que el frontend no supiera mostrar).

### Cómo probar localmente
`alembic upgrade head` (trae la migración `0012_badge_templates`), entrar a un evento como `coordinador`+ → tarjeta "Escarapelas" → el editor arranca con una plantilla por defecto. Ver `TESTING.md` sección 12 para el checklist completo (31 casos).

## Sprint 2.2 (2026-09-16) — Rediseño de Directorio/Registro (Fase A: columnas, modal, borrado real, estado manual)

Tras probar Sprint 2 en local, Juan David pidió un rediseño grande de la UX de Registro/Directorio en un solo mensaje (ver plan completo en la sesión — 4 fases). Esta es la Fase A, ya cerrada.

**Columnas de la tabla reducidas** (`static/js/directory.js: FIELDS_ORDER`): antes 10 columnas obligaban a hacer scroll horizontal. Ahora solo `ID, Nombres, Apellidos, Tipo de Asistente, Estado, Acción` — el resto (`role, company, phone, email` + los `opcional_N`) se ve/edita SOLO dentro del modal de "Editar".

**"Editar" abre un modal flotante** (`buildEditModal()` en `directory.js`) en vez de `contentEditable` en la fila — mismos campos que el alta manual, más los opcionales dinámicos de este evento (vía `window.OPTIONAL_VARIABLES`, inyectado en `kiosk_registro.html` igual que en `badge_editor.html`). `GET /api/users` (`get_all_users`) ahora también devuelve `extra_fields` por persona, necesario para precargar esos campos en el modal. `PATCH /api/users/{id}` acepta un nuevo campo `extra_fields` (dict) en el body y lo mezcla (merge, no reemplaza) con lo que la persona ya tenía — antes ese campo no existía en el contrato del endpoint.

**"Eliminar" ahora sí borra de verdad** (antes solo reseteaba el estado). Nuevo `DELETE /api/users/{user_id}?event_id=...` (**`admin`+**, mismo mínimo que ya exigía el viejo `.../logs`):
- Borra `EventAttendee`/`AccessLog` de esa persona, pero SCOPED a este evento (`event_id`) — a diferencia del viejo `DELETE /users/{id}/logs`, que por bug afectaba TODO el tenant (ese endpoint se deja documentado/vivo por compatibilidad, pero ya no se llama desde ningún lado del frontend).
- Si tras eso la persona no queda en NINGÚN otro evento del mismo tenant (`User` se reusa entre eventos, ver más abajo), se borra también el `User` y su foto física (`data/<tenant>/known_people/<id>.jpg`) — queda "eliminada de la base" de verdad. Si sigue en otro evento, el `User` se conserva intacto — no se puede reventar los datos de un evento ajeno solo porque se borró de este.
- Verificado con `TestClient`: una persona en 1 solo evento se borra completo; una persona en 2 eventos solo desaparece del evento donde se pidió borrar, sigue intacta en el otro.

**Nuevo: "Cambiar estado de registro"** — `PATCH /api/events/{event_id}/users/{user_id}/status` (**`admin`+**), `{"status": "registrado"|"no_registrado"}`, vive dentro del mismo modal de "Editar" (visible solo para `admin`+, ver más abajo). A "registrado" crea un `AccessLog(record_type="Existente")` si no había uno ya para este evento; a "no_registrado" borra los `AccessLog` de esa persona PARA ESTE EVENTO (deja `EventAttendee` intacto — sigue en el directorio). Pensado para el caso real que describió Juan David: cargar a alguien de antemano sin que quede "Registrado" hasta que de verdad llegue.

**Nivel de acceso de "Eliminar"/"Cambiar estado"**: Juan David dijo primero "para administradores y coordinadores" y después "solo para admins" — se resolvió a favor de **`admin`+ únicamente**, porque así ya estaba el backend del viejo `DELETE .../logs` (`require_role("admin")`) desde antes de este cambio — es el precedente real del código, no una decisión nueva. El botón "Editar" en sí sigue siendo `coordinador`+ (`EDIT_ROLES`, sin cambios) — un `coordinador` puede editar el perfil normal, pero dentro del modal NO ve el selector de estado ni el botón Eliminar (esos dos bloques del modal solo se renderizan si `ADMIN_ROLES.includes(window.STAFF_ROLE)`).

**Confirmación antes de aplicar**: tanto "Eliminar" como "Cambiar estado" disparan su propio `showConfirm` independiente del botón "Guardar cambios" del resto del modal (pedido explícito) — no hace falta guardar el perfil para poder aplicar cualquiera de los dos.

**Se quitó el botón "Acreditar" por fila.** Acreditar ahora pasa por el flujo de escaneo/búsqueda (ver Fase B) o por "Cambiar estado de registro" dentro de Editar. Los botones que quedan en cada fila son exactamente: 🖨️ Imprimir + Editar.

**Contador ampliado**: junto al botón "⚠️ N sin registrar" de siempre, ahora hay un segundo indicador de solo lectura "✅ X registrados de Y" (Y = total de personas de este evento, roster + altas manuales — mismo `allUsers.length` que ya se cargaba, sin queries nuevas).

## Sprint 2.2 (2026-09-16) — Fase B: unificación de pestañas + modo autoregistro

**"Registro Individual" y "Directorio en Vivo" se unifican en una sola pestaña "Registro"** (`templates/kiosk_registro.html`) — el formulario de alta manual deja de ser una pestaña propia y pasa a vivir en un modal flotante (`#registerModal`), abierto con el nuevo botón "➕ Registrar nuevo" o automáticamente cuando una búsqueda/escaneo de cédula no encuentra a nadie (`goToManualWithCedula`, sin cambios de fondo, solo ahora abre el modal en vez de hacer clic en una pestaña que ya no existe). El botón "🖨️ Imprimir Escarapela" dedicado del alta manual (`printManualBtn`) se quitó — ya no hace falta, el botón 🖨️ persistente por fila del Directorio cubre la misma necesidad de reimprimir.

**"Exportar Reporte" y "Usuarios del Evento" dejan de ser pestañas** dentro de Registro — se mueven a rutas propias: `GET /kiosk/{event_id}/estadisticas` (`kiosk_estadisticas.html`, por ahora solo el botón de exportar de siempre — el módulo de gráficos llega en la Fase D) y `GET /kiosk/{event_id}/usuarios` (`kiosk_usuarios.html`, contenido idéntico al que tenía la pestaña vieja). Ambas con `min_role="coordinador"`, mismo mínimo que exigían las pestañas viejas. Aparecen como tarjetas nuevas en `kiosk_select.html` (📊 Estadísticas, 👥 Usuarios del Evento), mismo patrón que "Escarapelas".

**Modo autoregistro (`Event.auto_register`, migración `0013_event_auto_register`)** — pedido explícito: un match facial o de cédula (por CUALQUIER método: escáner, barcode viejo, foto/MRZ nueva) YA NO acredita solo. `POST /api/recognize` y `POST /api/checkin-cedula` ganan un parámetro `confirm: bool = Form(False)`:
- Si hay match y (`event.auto_register` está prendido O vino `confirm=true`) → acredita de una, como se comportaba todo antes de este cambio.
- Si hay match y `event.auto_register` está apagado y no vino `confirm` → responde `result: "MATCH_PENDING"` con los datos de la persona, **sin crear ningún `AccessLog` todavía**.
- El frontend, al ver `MATCH_PENDING`, muestra la misma tarjeta de confirmación de siempre (`#profileCard`/`#liveEditForm` para el escáner facial; un modal nuevo y liviano, `showMatchConfirmModal()` en `directory.js`, para cédula) con un botón **"Guardar y Autorizar Acceso"** — al hacer clic, se reenvía la MISMA petición (mismo `FormData`, con la foto para `recognize`) agregando `confirm=true`, y recién ahí se crea el log real y se dispara `BadgePrint.maybeAutoPrint()`.
- El flujo `DUPLICADO`/`force` de siempre no cambió — se evalúa ANTES de este chequeo nuevo, en los dos endpoints.
- Switch visible como botón "🔓/🔒 Modo autoregistro" en la pestaña "Registro" (coordinador+), mismo patrón que el switch de auto-impresión (PATCHea `Event.auto_register` vía el `PATCH /api/events/{id}` genérico).
- **Distinto de "Cambiar estado de registro" de la Fase A**: ese es un override manual admin+ sin necesidad de ningún escaneo; esto es sobre qué pasa automáticamente cuando SÍ hay un escaneo/búsqueda con match. El modal de confirmación de cédula (`showMatchConfirmModal`) está disponible para cualquiera que pueda acreditar (digitador+), no solo admin+ — es la forma normal en que un digitador acredita a alguien encontrado por escaneo, ahora con un paso explícito de por medio.

Verificado con `TestClient`: `auto_register=False` → `checkin-cedula` da `MATCH_PENDING` sin tocar la base, y `confirm=true` sí acredita; `auto_register=True` → acredita directo como antes, sin pasar por `MATCH_PENDING`.

## Sprint 2.2 (2026-09-16) — Fase C: captura por cámara + OCR robusto a orientación

**Widget compartido de cámara** (`static/js/camera-capture.js`, nuevo) — `window.CameraCapture.open({title, onCapture(blob)})`: abre un modal propio con `getUserMedia`/`<video>`/`<canvas>`, distinto del `<video>` fijo del escáner facial (`app.js`) porque este se abre y cierra bajo demanda en vez de quedar prendido toda la sesión. Se usa en dos lugares nuevos, sin tocar el escáner facial existente:
- Junto al escaneo de cédula nueva (`kiosk_registro.html`): ahora hay dos botones, "📎 Adjuntar imagen" (el de siempre, ya no fuerza `capture="environment"`) y "📸 Usar cámara" — ambos terminan en la misma función `processMrzPhoto(blob)` que llama a `POST /api/events/{id}/cedula-mrz-scan`.
- Junto al campo de foto del alta manual en eventos biométricos: "📸 Usar cámara" captura y llena el `<input type="file" id="regPhotoInput">` vía `DataTransfer` (`dt.items.add(new File([blob], ...)); input.files = dt.files;`) — el resto del formulario (`submitManualRegister` en `app.js`) no se tocó, sigue leyendo `formData` del `<input>` exactamente igual sea que el archivo vino de adjuntar o de la cámara.

**OCR robusto a orientación** (`app/routers/cedula.py: scan_cedula_mrz`) — antes se asumía que la foto venía en una sola orientación; ahora se prueban las 4 rotaciones posibles (`np.rot90(img_array, k)` para `k=0,1,2,3`) y se usa la PRIMERA que produzca una lectura MRZ con checksum válido (`parse_mrz_td1(...).valid`), deteniéndose ahí mismo sin seguir probando las demás. Si NINGUNA rotación detecta siquiera 3 líneas de texto, el mensaje de error es distinto ("no se detectó la zona MRZ") al de "se detectaron líneas pero el checksum no cuadra" — ayuda a saber si el problema es el encuadre/luz o una lectura parcialmente mala. Verificado con un mock de `extract_mrz_lines` que solo "ve" la MRZ en la segunda rotación probada: el endpoint la encuentra igual y se detiene ahí (no sigue probando las rotaciones 3 y 4 de más).

## Sprint 2.2 (2026-09-16) — Tercera ronda de feedback

**Bug real: la pestaña "Estadísticas" de `cliente` tenía DOS secciones activas a la vez** (`kiosk_registro.html`) — tanto `#estadisticas` como `#directorio` traían `class="section active"` hardcodeada al mismo tiempo (bug pre-existente, no introducido en esta ronda). Además, "Estadísticas" solo tenía un botón "Ver Estadísticas" que sacaba a otra página, así que no se podía alternar entre Estadísticas y Directorio sin perder el lugar. Arreglo: solo `#estadisticas` (la que coincide con la pestaña marcada `active`) empieza activa; `#directorio` se activa recién al hacer clic en su pestaña (la lógica genérica de `app.js` ya sabe hacer esto bien, el problema era el HTML inicial). "Estadísticas" ahora incrusta la página real (`/kiosk/{event_id}/estadisticas?embed=1`, ver abajo) en un `<iframe>` en vez de un botón — el contenido real (gráficos + exportar) ya está ahí sin dar clic a nada más, y sí se puede rotar libremente entre las dos pestañas.
- `?embed=1` (nuevo, `kiosk_estadisticas.html` + `main.py: kiosk_estadisticas`): omite el header/franja de contexto propios de esa página cuando se abre incrustada (ya los tiene la página contenedora) — se sigue pudiendo abrir la página normal, sin el parámetro, con su apariencia completa de siempre.

**Validación de formato al cargar la base de asistentes** (`app/routers/api.py: bulk_register`) — pedido explícito: antes, un archivo con columnas totalmente distintas a la plantilla se procesaba igual, y cada fila terminaba reportada como "❌ sin ID/cédula" sin dejar claro que el problema real era el FORMATO del archivo. Ahora, antes de procesar cualquier fila, se valida que el archivo tenga al menos las columnas `id`/`cédula`, `nombres` y `apellidos` (cualquiera de sus variantes reconocidas) — si no, `400` con un mensaje claro pidiendo usar la plantilla oficial, sin intentar procesar nada.

**Validación: no se puede devolver un evento a "Creado" si ya tiene gente cargada** (`app/routers/events.py: update_event`) — pedido explícito. Se agregó un chequeo: si `data.status == "creado"` y el evento ya tiene al menos un `EventAttendee` (roster cargado y/o gente que ya se registró en vivo), `400` explicando que "Creado" es preparación previa y no tiene sentido volver ahí con datos reales adentro — sugiere "Finalizado" para pausar, o crear un evento nuevo si de verdad hace falta empezar de cero. Aplica viniendo de cualquier estado con asistentes, no solo desde "En Proceso".

Verificado con `TestClient`: archivo con columnas random → 400 sin tocar la base; archivo con formato correcto → 200 normal; evento con asistentes → 400 al intentar volver a "creado" (probado también viniendo de un evento recién creado que justo acabó de recibir su primera carga); evento sin ningún asistente → sigue pudiendo ir a "creado" sin problema. El toggle de pestañas de `cliente` se verificó en vivo en el navegador: "Estadísticas" carga los gráficos reales de una, y alternar con "Directorio en Vivo" funciona en ambos sentidos sin perder el lugar.

## Sprint 2.2 (2026-09-16) — Segunda ronda de feedback

**Bug real: el fondo de color sólido del editor de escarapelas no funcionaba** (`badge_editor.html`) — `document.getElementById('bgType').addEventListener('change', toggleBgInputs)` solo cambiaba qué input se veía (color vs imagen), pero NUNCA actualizaba `template.background_type`/`background_value` en memoria ni volvía a dibujar el lienzo — eso solo pasaba al guardar. Y el `<input type="color">` (`#bgColor`) no tenía NINGÚN listener propio. Resultado: elegir "Color sólido" después de haber puesto una imagen dejaba la imagen visible en el lienzo (parecía que "no funcionaba" o que "se mantenía la imagen"). Arreglo: los tres controles de fondo (`bgType`, `bgColor`, `bgImageInput`) ahora actualizan el modelo y llaman `renderCanvas()` al instante, igual que ya hacía la subida de imagen. Verificado en vivo: cambiar a un color se ve al instante; poner una imagen y volver a color revierte correctamente al color (ya no se queda la imagen).

**Botón de "Auto-impresión" se mueve de Escarapelas a Registro** — pedido explícito: vive mejor junto a "Modo autoregistro" (ambos son comportamiento de cómo se acredita/imprime en Registro, no de diseño de la escarapela). Se quitó de `badge_editor.html` (toolbar + JS) y se agregó a `kiosk_registro.html`, en la misma fila que el switch de autoregistro — mismo campo/endpoint de siempre (`Event.auto_print_badge` vía `PATCH /api/events/{id}`), solo cambió dónde vive el botón.

**Columna "Acción" del Directorio pasa a ser la PRIMERA** (`static/js/directory.js: buildRow`, `kiosk_registro.html`'s `<thead>`) — antes al final, obligaba a desplazarse hasta el final de cada fila para Editar/Imprimir. Ahora es la primera columna, seguida de ID/Nombres/Apellidos/Tipo Asistente/Estado en el mismo orden de siempre.

**Barra de contexto "dónde estoy" ahora es mucho más evidente** (`static/css/style.css: .golden-event-strip`) — pedido explícito tras la primera versión (la encontraron poco visible): tipografía más grande, etiquetas ("CLIENTE"/"EVENTO"/"CÓDIGO") en mayúsculas doradas apiladas arriba de cada valor, borde inferior dorado de 3px y sombra — imposible no verla al entrar a cualquier pantalla de un evento.

**Estadísticas habilitado para `cliente`** — antes la ruta de página (`GET /kiosk/{event_id}/estadisticas`) exigía `min_role="coordinador"`, y encima `cliente` nunca pasaba por `kiosk_select.html` (su `kiosk_entry` lo manda directo a `kiosk_registro.html`), así que seguía viendo el placeholder viejo ("Próximamente. Esta sección todavía no está diseñada") sin importar lo que se hiciera en la página real.
- `app/auth.py`: nueva `require_role_or_client(minimum_role)` — dependencia que deja pasar a `coordinador`+ Y a `cliente` específicamente, pero sigue bloqueando `digitador` (que en `STAFF_ROLES` queda POR ENCIMA de `cliente` en la jerarquía, así que un simple `min_role` no puede expresar "cliente sí, digitador no"). Se usa en `GET /api/events/{id}/stats/variables`, `GET /api/events/{id}/stats/data` y `GET /api/report` (los tres que necesita la página de Estadísticas).
- `app/main.py: _resolve_kiosk_page` gana un parámetro `exclude_roles: list` (en vez de `min_role`, que es jerárquico) — `kiosk_estadisticas` ahora usa `exclude_roles=["digitador"]`.
- `kiosk_registro.html`: la pestaña "Estadísticas" de `cliente` deja de ser un placeholder — ahora tiene un botón "📊 Ver Estadísticas" que lleva a la página real (`/kiosk/{event_id}/estadisticas`), la misma que usa `coordinador`+.
- Verificado con `TestClient` usando un login real (sesión de verdad, no solo el dependency override de la API): `cliente` → 200 en la página y en los 3 endpoints; `digitador` → 302/403 en los cuatro; `coordinador` → 200 sin cambios.

## Sprint 2.2 (2026-09-16) — Ronda de feedback tras probar las 4 fases

**Librería de plantillas de Escarapelas pasa a ser GLOBAL, no por tenant** (`app/routers/badges.py`) — pedido explícito: antes `GET .../saved-badge-templates` y `POST .../import/{id}` filtraban por `SavedBadgeTemplate.tenant_id == event.tenant_id`, así que una plantilla guardada desde el evento de un cliente no aparecía ni se podía importar en el evento de OTRO cliente. Ahora se lista/importa sin ningún filtro de tenant — cualquier plantilla guardada por cualquiera, en cualquier evento de cualquier cliente, es visible e importable desde cualquier otro evento. La columna `tenant_id` se sigue guardando en la fila (trazabilidad de quién la creó), simplemente ya no filtra. Las imágenes referenciadas (`background_value`/`storage_path`) siguen resolviendo bien entre tenants porque `GET /api/badge-assets/{tenant_id}/{filename}` ya recibía el `tenant_id` como parte de la ruta, no lo inferido de la sesión. Verificado con `TestClient`: plantilla guardada desde el evento del Tenant A aparece y se importa correctamente en un evento del Tenant B.

**Gráficos de Estadísticas: colores distintos por categoría también en barras** (`kiosk_estadisticas.html: buildChartConfig`) — antes solo el gráfico circular (pie) coloreaba cada porción distinto; el de barras pintaba todas las barras del mismo color sólido. Ahora barras y circular comparten la misma paleta por categoría (`CHART_PALETTE`); el de líneas sigue usando un solo color (correcto para una serie continua, no categorías).

**Directorio/Registro y el resto de la app ganan diseño responsive real** (`static/css/style.css`) — antes el archivo no tenía NINGÚN `@media query`. Cambios:
- `.golden-container`/`.golden-header-container` pasan de `max-width:1200px` a `1600px`, y `.app-container` (donde vive la tabla del Directorio) de `1000px` a `1500px` — en monitores anchos ya no queda todo apretado en una columna central angosta ("cuadrado"); en pantallas más chicas que el máximo, sigue ocupando el 100% disponible como siempre.
- Nuevo quiebre `@media (max-width: 900px)`: reduce paddings/tamaños de título.
- Nuevo quiebre `@media (max-width: 640px)`: el header deja de ser `position:fixed` (pasa a `static`) y el spacer se oculta — en vez de calcular a mano cuántas líneas puede ocupar un header con muchos ítems de navegación en un celular angosto (y arriesgarse a que tape contenido), simplemente fluye como bloque normal, sin importar a cuántas líneas se envuelva. Tabla del Directorio con fuente/padding más chico (sigue con scroll horizontal propio como respaldo, ya existía). Los dos botones nuevos de cámara/adjuntar (cédula MRZ y foto de alta manual) ganan `flex-wrap`/`flex-basis` para apilarse en una sola columna en vez de recortarse.
- Verificado en vivo con el emulador de móvil del navegador (375×812): el header se ve completo sin recortes, los botones de escaneo se apilan bien, la tabla es legible con scroll horizontal propio para las columnas que no caben.

**Barra de contexto "dónde estoy"** (`.golden-event-strip`, nueva franja fija debajo del header) — pedido explícito: "para saber siempre donde estoy". Muestra `Cliente: {tenant.name}` · `Evento: {event.name}` · `Código: {event.event_code}`, presente en TODAS las páginas dentro de un evento (`kiosk_registro.html`, `kiosk_roster.html`, `badge_editor.html`, `kiosk_estadisticas.html`, `kiosk_usuarios.html`, `kiosk_select.html`). Reemplaza el viejo badge suelto que solo mostraba `event.name` dentro del nav del header (se quitó de ahí para no competir con la franja nueva y para aliviar un poco el header, que ya venía cargado de enlaces).

## Sprint 2.2 (2026-09-16) — Fase D: Escarapelas (lienzo), spinners de carga y módulo de Estadísticas

**Lienzo del editor de escarapelas más chico** (`badge_editor.html`): `EDITOR_SCALE` bajó de `5` a `3` — con 5 el lienzo (62×100mm × 5 = 310×500mm) no cabía en pantalla sin hacer scroll. El arrastre/redimensión no dependen de este número (derivan el ratio px/mm de `canvasEl.getBoundingClientRect()` en cada momento), así que el cambio es puramente visual. `.badge-canvas-wrapper` ganó `max-width:100%` para no desbordar en pantallas angostas.

**Los 2 bugs reportados del editor (selector de variable / no dejaba adjuntar foto) se investigaron EN VIVO** (servidor real + navegador, no solo lectura de código) antes de tocar nada — **ninguno de los dos se reprodujo**:
- El selector de "Variable" de un `text_variable` funciona correctamente: se probó cambiándolo de verdad en el navegador (de "Nombres" a "Empresa") y el lienzo se actualizó al instante.
- La subida de imagen de un `image_static` funciona correctamente: se simuló adjuntar un archivo real y `POST /api/events/{id}/badge-template/upload-image` respondió 200 y la imagen quedó asociada al elemento.
- Lo que sí se confirmó es que **`image_variable` ("Foto del asistente") nunca tuvo ni debía tener un control de subir imagen** — es la foto real de cada persona, se resuelve sola al imprimir (por diseño, no un bug). Se mejoró el texto explicativo de ese elemento en el panel de propiedades para dejarlo más claro (aclara explícitamente que no hay botón de subir ahí, y sugiere usar "🖼️ Imagen / logo" si lo que se quería era subir un logo/imagen fija) — es la hipótesis más probable de qué generó el reporte original.
- **Conclusión:** no se tocó la lógica de estos dos flujos porque ya funcionaban — si el problema persiste en el entorno real de Juan David, probablemente sea caché del navegador con una versión vieja del JS (`StaticFilesNoCacheInDev` ya desactiva el caché en `development`, pero conviene un hard-refresh igual) o el malentendido de `image_variable` de arriba.

**Spinners de carga** (`static/js/toast.js: window.setButtonLoading(btn, isLoading, loadingText)`) — reemplaza el patrón manual de `btn.innerText = "Guardando..."; btn.disabled = true` repetido en varios lugares por un círculo girando (CSS `@keyframes`) + texto, para distinguir "está funcionando" de "parece colgado". Guarda el HTML original del botón en `dataset.originalHtml` y lo restaura solo. Aplicado en: subir roster (`kiosk_roster.html`), guardar plantilla de escarapela (`badge_editor.html: saveTemplate`), subir imagen de escarapela (inline en el listener de `p_image`), escanear MRZ (`kiosk_registro.html: processMrzPhoto`), alta manual y confirmación de acceso facial/cédula (`app.js`).

**Módulo de Estadísticas** (`/kiosk/{event_id}/estadisticas`, `kiosk_estadisticas.html`) — mantiene el botón de exportar Excel de siempre, y agrega:
- `GET /api/events/{event_id}/stats/variables` (`app/routers/stats.py`, `coordinador`+) — devuelve las variables graficables: fijas (`company`→"Empresa", `role`→"Cargo", `opt_1`→"Tipo de Asistente", `status`→"Estado de registro") + cada `opcional_N` que el evento tenga rotulado. **Se excluyen a propósito `id`/`first_name`/`last_name`/`phone`/`email`** — son identificadores prácticamente únicos por persona, un gráfico con 500 barras distintas no sirve. (Si Juan David de verdad los quiere disponibles, es un cambio de una línea en `_FIXED_VARIABLES` — se avisa en la entrega.)
- `GET /api/events/{event_id}/stats/data?variable=X` (mismo router) — sobre el mismo universo de personas que el Directorio de ese evento (`EventAttendee ∪ AccessLog` por `event_id`), detecta el tipo de la variable por sus VALORES reales (no por un tipo de columna fijo): si ≥90% de los valores no vacíos parsean como número → `"numeric"` (histograma de 8 bins entre el mínimo y el máximo); si no → `"categorical"` (conteo por valor exacto, top 15 + "Otros" agrupado para no romper el gráfico con demasiadas categorías).
- Frontend: `<select>` de variables (checklist "una por una") + "➕ Agregar variable" → por cada una, una tarjeta con su gráfico (**Chart.js 4.5.1 vía CDN `cdnjs`** — no había ninguna librería de gráficos en el proyecto antes de esto) y un `<select>` de tipo de gráfico acorde al `type` detectado (categórica → Barras/Circular; numérica → Histograma/Líneas) — cambiar el tipo re-dibuja el mismo gráfico sin recargar los datos.
- **Alcance de esta entrega: una variable a la vez** (confirmado con Juan David) — cruces de 2+ variables (ej. pirámide edad×género) quedan para una fase futura, ya que necesitan un segundo selector y una lógica de agregación distinta (conteo cruzado, no un solo `Counter`).
- Verificado en vivo (servidor real + navegador): variable categórica ("Empresa") renderiza barras y circular correctamente con los conteos reales; variable numérica ("Edad", vía `opcional_1`) renderiza el histograma con los bins correctos; sin errores de consola.

## Sprint 2.2 (2026-09-16) — Cuarta ronda de feedback: Parámetros del Evento

**Nueva sección "⚙️ Parámetros del Evento"** (`/kiosk/{event_id}/parametros`, `min_role="coordinador"`, tarjeta nueva en `kiosk_select.html`) — pedido explícito: coordinador/admin pueden personalizar, por evento, cómo se comporta cada campo del alta manual/edición (cuáles son obligatorios, qué tipo de control es, y cuáles generan su gráfico solo en Estadísticas sin tener que pedirlo a mano).

- **Modelo nuevo** (`app/models.py: EventFieldConfig`, migración `0014_event_field_configs.py`) — una fila por `(event_id, field_key)`: `required` (bool), `field_type` (`text_short` | `text_long` | `select` | `boolean`), `options_json` (lista JSON, solo aplica si `field_type="select"`), `default_stat_enabled` (bool) + `default_chart_type` (`bar`|`pie`|`histogram`|`line`, solo si lo anterior es `true`). Sin fila para un campo dado = valores por defecto (no obligatorio, texto corto, sin estadística) — así el frontend siempre ve la lista completa de campos configurables, nunca "filas faltantes".
- **Campos configurables**: las 5 variables fijas del formulario (`role`→Cargo, `company`→Empresa, `phone`→Teléfono, `email`→Correo, `opt_1`→Tipo de Asistente) + cada `opcional_N` que el evento ya tenga rotulado (`Event.optional_field_labels`). La identidad (`id`/`first_name`/`last_name`) queda fuera a propósito — siempre texto corto obligatorio, no configurable. `status` tampoco se configura acá (no es un campo de formulario, solo existe como variable de Estadísticas).
- **Backend** (`app/routers/parametros.py`, nuevo): `GET /api/events/{id}/field-configs` (lista completa, real+defaults) y `PUT /api/events/{id}/field-configs/{field_key}` (upsert; valida `field_type` contra el enum y exige `options` no vacío si el tipo es `select`; rechaza una `field_key` que no sea configurable en ese evento). `field_configs_for_event(db, event)` es la función compartida que reutilizan `main.py` (para inyectar `window.FIELD_CONFIGS`), `api.py` (validación de obligatorios) y `stats.py` (defaults de gráfico).
- **Decisión de alcance avisada en la entrega**: lo "obligatorio" se valida en `manual_register` y `update_user` (PATCH) — **no** en `bulk_register` (carga de Excel), que sigue con su comportamiento tolerante de siempre. Forzarlo ahí arriesgaba romper cargas reales con datos incompletos que hoy funcionan, y no fue lo que se pidió explícitamente.
- **Formulario dinámico compartido** (`static/js/field-render.js`, nuevo — `window.FieldRender.renderControl(config, value)`) — genera el `<input>`/`<textarea>`/`<select>`/checkbox correcto (con `required` nativo del HTML5 cuando aplica) a partir de un `field_config`. Reemplaza los `<input>` fijos que antes tenían tanto el modal "Registrar nuevo" (`kiosk_registro.html`, ahora renderizado server-side desde `field_configs` vía Jinja) como el modal "Editar" del Directorio (`static/js/directory.js: buildEditModal`, ahora envuelto en un `<form>` propio para poder llamar `reportValidity()` antes de guardar — el modal no es un `<form>` que se envíe solo, así que la validación nativa no se dispara sin pedirla a mano). Un campo `select` sin opción elegida, o un `boolean` obligatorio sin marcar, bloquea el guardado con el propio mensaje nativo del navegador.
- **Estadísticas por defecto** (`app/routers/stats.py: list_stats_variables` + `kiosk_estadisticas.html`) — cada variable devuelta ahora trae `default_stat_enabled`/`default_chart_type`; al cargar la página, cualquiera marcada `default_stat_enabled=true` se agrega sola (mismo `addVariableChart` de siempre) sin tocar "➕ Agregar variable", que sigue funcionando igual para el resto. El selector de tipo de gráfico ofrece 4 opciones (Barras/Circular/Histograma/Líneas) porque en Parámetros no se sabe de antemano si el dato real va a resultar numérico o por categorías; al momento de dibujar, `resolveChartType(requested, dataType)` normaliza `"histogram"` a `"bar"` (que YA es el histograma para datos numéricos, Chart.js no tiene un tipo aparte) y, si el tipo pedido no aplica al dato real detectado (ej. se configuró "Circular" pero los datos resultaron numéricos), cae al primero válido para ese tipo — nunca rompe el gráfico.
- Verificado con `TestClient` (17 casos: defaults, upsert con/sin opciones válidas, `field_key` inválido rechazado, `manual_register`/`update_user` rechazando con 400 cuando falta un campo obligatorio y aceptando cuando viene completo, `stats/variables` reflejando `default_stat_enabled`/`default_chart_type`, `digitador` recibiendo 403 en ambos endpoints nuevos), sweep de render Jinja de `kiosk_registro.html`/`kiosk_parametros.html` con combinaciones de tipos de campo (vacío, select, booleano, texto largo, mezcla completa) y los 4 roles de staff, `node --check` en los JS tocados, y verificación en vivo en navegador (servidor real + SQLite): un campo `select` obligatorio bloquea el modal de alta sin elegir opción y lo deja pasar al elegirla; el mismo campo bloquea "Guardar cambios" en el modal de Editar del Directorio si se vacía; una variable marcada "Generar estadística" con tipo "Circular" aparece sola en Estadísticas (sin usar "Agregar variable") y resuelve al gráfico circular real una vez hay datos categóricos cargados.

## Sprint 2.4 (2026-09-16) — Fase 0: rol "comercial" y matriz de permisos

Pedido grande de varias fases (ver PR/commits de esta fecha en adelante) — esta primera fase agrega el rol `comercial`. `STAFF_ROLES` (`app/models.py`) pasa a `("cliente", "digitador", "coordinador", "comercial", "admin", "super_admin")` — sin migración de esquema, es un valor de texto en una columna que ya existía. Insertarlo entre `coordinador` y `admin` hace que la mayoría de sus permisos se hereden solos vía `require_role(...)`:
- **`comercial` ahora crea clientes (tenants) y eventos** (`create_tenant`/`create_event`, mínimo subió de `coordinador` a `comercial`) — **`coordinador` PERDIÓ esta capacidad**, sigue pudiendo editar/gestionar lo que ya existe (estado, roster, escarapelas, Estadísticas/Reporte, Parámetros) sin cambios, heredado igual que siempre.
- **Dos excepciones que NO siguen la jerarquía simple**, codificadas a mano en `create_event_staff` (`app/routers/events.py`): `comercial` NO puede crear cuentas `digitador` (aunque quede "por encima" de `coordinador`); sí puede crear cuentas `cliente` (antes solo admin+).
- `app/routers/staff.py`: `ROLES_CREATABLE_BY_ADMIN` y `DELETABLE_ROLES_BY` ganan `"comercial"` — admin+ lo crea desde `/admin/staff` igual que `coordinador`.
- `StaffUser.phone` (nuevo, nullable, migración `0015_staff_phone`) — para notificaciones por WhatsApp en una fase futura del mismo sprint. Formularios de creación de staff (`staff.html`, `kiosk_usuarios.html`) ganan el campo opcional.
- Frontend: `kiosk_usuarios.html` oculta "Digitador" del selector de rol cuando `staff_role == "comercial"`, muestra "Cliente" para `comercial`/`admin`/`super_admin`. `clientes.html` oculta "+ Nuevo cliente"/"+ Nuevo evento" para `coordinador` (`const canCreate = staff_role !== "coordinador"`).
- Verificado con `TestClient` (15 casos: coordinador ya no crea tenant/evento pero sigue editando; comercial crea tenant/evento/cliente pero no digitador; comercial hereda acceso a Estadísticas/Reporte/cambio de estado; admin crea cuenta comercial) + sweep de render Jinja en los 4 roles + `node --check`. Verificado en vivo: cuenta comercial real creada desde `/admin/staff`, selector de rol en "Usuarios del Evento" correcto, botón "+ Nuevo cliente" ausente para coordinador.

## Sprint 2.4 (2026-09-16) — Fase 1: ajustes de UI/UX

- **"Crear evento" pasa a ser pantalla dedicada** (`/clientes/{tenant_id}/nuevo-evento`, `templates/nuevo_evento.html`, comercial+) — reemplaza el formulario inline que aparecía bajo el cliente expandido en `/clientes`. `clientes.html` se simplificó bastante (se fue todo el JS de coordinador/país/ciudad/clockpicker que solo servía para ese formulario).
- **"+ Agregar campo opcional" se quitó del alta manual** (`kiosk_registro.html`) — agregar campos nuevos queda exclusivamente en Parámetros del Evento.
- **Ejemplos al preguntar "¿a qué corresponde Opcional N?"** — `bulk_register` (`app/routers/api.py: _optional_field_examples`) agrega hasta 2 valores reales del archivo a la respuesta `NEEDS_LABELS`; `window.promptOptionalLabels` (`toast.js`) los muestra bajo cada input.
- **Tesseract en Windows local**: `app/mrz_ocr.py` autodetecta el binario en las rutas por defecto del instalador de UB Mannheim (`C:\Program Files\Tesseract-OCR\tesseract.exe`) si `tesseract` no resuelve por PATH — no hace falta tocar variables de entorno, solo instalar el `.exe`. (Prod ya estaba resuelto desde Sprint 2.3, vía `deploy.yml`.)
- **Apariencia disponible para todos los roles** — `/configuracion` deja de exigir admin+ (cualquier staff logueado entra); la pestaña "Staff y Permisos" sigue oculta para no-admin dentro del template. `digitador`/`cliente` (sin sidebar) ganan un ícono ⚙️ en el header de `dashboard.html`/`kiosk_registro.html` para llegar ahí. El selector de tipografía pasó de 4 combinaciones fijas a las mismas 40 familias de Google Fonts que ya ofrece el editor de escarapelas (`window.GOLDEN_FONT_FAMILIES`, expuesto por `theme.js`) — **bug real encontrado y corregido en la misma verificación**: el `<link>` de Google Fonts solo se inyectaba en la carga siguiente de la página (la lógica vivía solo dentro del IIFE de `theme.js`, que corre una única vez al cargar); se expuso `window.goldenEnsureFontLink()` para que `configuracion.html` la llame también al aplicar un cambio en la misma sesión, sin necesitar recargar.
- **Cédula editable en el flujo normal de "Guardar cambios"** (`static/js/directory.js: buildEditModal`) — ya no es una mini-sección aparte; el campo de cédula del formulario principal deja de estar `disabled` para admin+, y "Guardar cambios" primero corrige la cédula (si cambió, con su propia confirmación) y recién después guarda el resto de campos con el id ya actualizado.
- Verificado con `TestClient` (22 casos: acceso a la pantalla dedicada por rol, tenant inexistente redirige, examples de `NEEDS_LABELS`, sweep de render en 6 roles) + `node --check` + navegador real (pantalla de nuevo evento con sidebar, coordinador bloqueado, selector de 41 opciones de fuente aplicando en vivo tras corregir el bug del `<link>`, botón de agregar campo ausente del alta sin errores de consola).

## Sprint 2.4 (2026-09-16) — Fase 3: batch de correcciones tras probar Fase 2 en vivo

Ronda de feedback grande tras probar las fases anteriores en local — varios bugs reales encontrados por el usuario, más 2 features chicas. Todo en `sprint2/reverify-fixes-and-badges`, sin merge a `main`.

**Bug real: matching de nombre por cédula solo funcionaba en un sentido.** `_matches_by_word_prefix` (usado por Fase 2 para el fallback de nombre) exige que cada palabra del QUERY sea prefijo de alguna palabra del HAYSTACK — funcionaba para "Sebas Angarita" (corto) buscando "Sebastián Angarita" (BD larga), pero fallaba en el caso reportado esta ronda: escaneo "JHOAN SEBASTIAN ANGARITA ROJAS" (largo, con palabras extra) contra BD "Sebas Angarita" (corta) — ningún lado es prefijo limpio del otro en ese sentido. Se agregó `_identity_name_matches(db_name, scanned_name)` (`app/routers/api.py`) — match bidireccional por palabra: cada palabra de la BD debe encontrar alguna palabra del escaneo que la tenga como prefijo O de la que sea prefijo (en cualquier dirección), en vez de exigir que TODAS las palabras del escaneo calcen. `_matches_by_word_prefix` (en `events.py`, usado por `/api/events/search`) se dejó intacto a propósito — es un caso de uso genuinamente distinto (búsqueda por texto parcial, no identidad).

**Bug real: "Modo autoregistro" apagado + cédula con match exacto igual acreditaba sola** — corrección directa sobre lo que se había entregado en la Fase 2 ("cédula exacta siempre acredita, el switch queda exclusivo de facial"), tras verlo en vivo: el usuario pidió que si el switch está apagado, un match de cédula debe dejar a la persona en blanco ("No registrado") en el Directorio, no en verde, hasta confirmar. `checkin_cedula` recupera un parámetro `confirm: bool = Form(False)`: con match exacto, si `event.auto_register` está prendido O viene `confirm=true`, acredita igual que antes; si está apagado y no viene `confirm`, responde `result: "FOUND_PENDING"` con los datos de la persona, **sin crear ningún `AccessLog`**. `static/js/directory.js: fastCheckin` filtra igual la fila (queda visible, en blanco) e inyecta un botón inline "✅ Acreditar" en esa fila (vía `tr.dataset.userId`, nuevo) que reintenta la misma petición con `confirm=true` — deliberadamente NO es un modal flotante aparte (ver la nota de alcance de Fase 2: cédula se comporta como una búsqueda, no como un popup).

**Bug real: formularios flotantes se cerraban al hacer clic afuera**, perdiendo lo escrito — pedido explícito de que solo cierren vía "Guardar"/"Cancelar". Se quitó el patrón `overlay.addEventListener('click', (e) => { if (e.target === overlay) close(); })` de los 3 formularios de datos que lo tenían: `registerModal` (`kiosk_registro.html`), el modal de "Editar" (`static/js/directory.js: buildEditModal`), y `window.showPrompt` (`static/js/toast.js`). **Deliberadamente sin tocar**: el modal de cámara (`camera-capture.js`, no es un formulario de datos) y `showConfirm` (`toast.js`, es un diálogo sí/no, no una entrada de datos) — se mantiene la distinción explícita entre "formulario que puede perder datos" y "diálogo de confirmación".

**Botón "🧹 Limpiar filtros"** — nuevo, junto a "➕ Registrar nuevo" en la sección Registro. `GoldenDirectory.mountSearch(opts)` gana `clearFilters()` en el objeto que devuelve (limpia los 3 campos de búsqueda + el filtro "solo sin registrar" + vuelve a aplicar filtros).

**Aviso de duplicado (registro e impresión) ahora incluye el conteo de veces anteriores** — pedido explícito ("y cuántas veces ha pasado antes"):
- `_duplicate_warning(db, event_id, user)` (antes recibía solo `user`) ahora también consulta `db.query(AccessLog).filter(...).count()` y agrega `"times_registered"` a la respuesta — usado por `recognize`, `checkin_cedula` y `manual_register`. `static/js/toast.js: confirmDuplicateRegistration(data, times)` arma el texto ("ya lo hizo N veces") y lo pasan los 3 call sites del frontend (`app.js` ×2, `directory.js`).
- **Nuevo, para impresión** (antes no existía ningún rastro de impresiones en la base — no había forma de avisar "ya se imprimió antes"): modelo `PrintLog` (`app/models.py`, migración `0016_print_logs`) — tabla separada de `AccessLog` a propósito, porque mezclar impresiones ahí rompería el cálculo de estado Nuevo/Registrado (que depende de `record_type`). `GET /api/users/{id}/print-count?event_id=` y `POST /api/users/{id}/print-log?event_id=` (`app/routers/badges.py`, `digitador`+, mismo mínimo que el resto de endpoints de impresión). `static/js/badge-render.js: BadgePrint.openPrintWindow(userId)` pasó a ser `async` — consulta el conteo antes de abrir la ventana; si ya se imprimió alguna vez, pide confirmación (`showConfirm` variante warning, con el conteo) antes de seguir; en cualquier caso (con o sin aviso previo) registra la impresión vía `POST print-log` antes de abrir la ventana. Si la consulta o el registro fallan (red, etc.), no bloquean la impresión — son best-effort, la función sigue e imprime igual.

Verificado con `TestClient` (10 casos nuevos en `print-count`/`print-log`/conteo de `checkin-cedula`, más los 12 ya existentes de Fase 2/bidireccional siguen pasando) + `node --check` en los 4 JS tocados + navegador real: primera impresión abre directo sin aviso; segunda impresión muestra el aviso con el conteo correcto (singular/plural), "Cancelar" no abre nada ni registra impresión, "Sí, imprimir de nuevo" registra y abre.

## Sprint 2.4 (2026-09-16) — Fase 4: teléfono obligatorio y único para cuentas de staff

Pedido explícito: "el teléfono al crear un usuario (excepto clientes y usuarios temporales digitadores) debe ser obligatorio y lo vamos a usar como llave única... revisa de una vez la base y si hay algún caso corrígelo".

- **`StaffUser.phone`** (columna ya existente desde Fase 0, `nullable=True`) gana `unique=True` (`app/models.py`) — migración `0017_staff_phone_unique.py` (`down_revision = "0016_print_logs"`). Un `UNIQUE` estándar no choca entre varios `NULL` (ni en Postgres ni en SQLite), así que no hizo falta un índice parcial — cuentas sin teléfono (el caso de `digitador`/`cliente`, la excepción del pedido) conviven sin problema.
- **Limpieza real de la base, dentro de la propia migración** (pedido explícito "revisa de una vez la base y corrígelo", no se puede hacer a mano porque no hay acceso directo a la base de producción desde esta sesión): antes de crear la restricción, `upgrade()` normaliza cadenas vacías (`""` → `NULL`) y, si dos o más cuentas comparten el mismo teléfono, conserva el de la cuenta más antigua (menor `id`) y vacía el resto — no se puede adivinar cuál de los dos números es el correcto sin preguntar, y quedar en `NULL` no rompe nada (el teléfono es opcional para operar, solo se usa para WhatsApp más adelante). Se corre automáticamente en el próximo `alembic upgrade head` (deploy a producción o `alembic upgrade head` local) — no hace falta ningún paso manual aparte.
- **Backend**: `POST /api/staff` (`app/routers/staff.py: create_staff`) — solo crea `coordinador`/`comercial`/`admin` (nunca `digitador`/`cliente`, esos se rechazan ahí mismo desde antes), así que el teléfono pasa a ser **obligatorio** (400 si viene vacío) y **único** (400 si ya lo tiene otra cuenta) sin ninguna excepción que codificar en este endpoint. `POST /api/events/{id}/staff-users` (`app/routers/events.py: create_event_staff`, crea `digitador`/`cliente`) — el teléfono sigue siendo **opcional** (la excepción del pedido), pero si de todas formas se manda uno, se valida que sea único antes de intentar el insert (evita un `IntegrityError` crudo de la restricción de base).
- **Frontend**: `templates/staff.html` — el campo de teléfono del formulario de "Nueva cuenta" (coordinador/comercial/admin) gana `required`. `templates/kiosk_usuarios.html` (digitador/cliente) se deja intacto, sigue opcional.
- Verificado con `TestClient` (8 casos: coordinador sin teléfono → 400, teléfono duplicado → 400, teléfono nuevo → 200, digitador/cliente sin teléfono → 200, digitador con teléfono duplicado → 400) + una prueba aparte de la lógica de dedupe de la migración contra una tabla SQLite sembrada a mano con duplicados/cadenas vacías (7 casos: se conserva el `id` más antiguo, el resto y las cadenas vacías quedan en `NULL`, un teléfono único no se toca, y el `UNIQUE INDEX` se crea sin error después del dedupe).

## Sprint 2.4 (2026-09-16) — Fase 5: comercial asignada a eventos + filtros de pertenencia

Pedido explícito: "cuando yo creo un evento, así como le puedo asignar un coordinador, también por defecto se asigne una comercial... si es una comercial [quien crea] lo puede cambiar a otras comerciales... o los administradores eligen una comercial... para la lógica de que en cuentas comerciales pueda filtrar los eventos, tanto en Eventos como en Calendario, por pertenencia (mis eventos/todos), y a los administradores agregarles el filtro de ver todos filtrando por cualquier comercial o coordinador asignado".

- **`Event.commercial_staff_id`** (nuevo, `app/models.py`, migración `0018_event_commercial`, `nullable=True` a nivel de columna — eventos viejos quedan sin uno, sin backfill posible) — mismo patrón que `coordinator_staff_id`: FK a `StaffUser`, `relationship("StaffUser", foreign_keys=[commercial_staff_id])`.
- **`POST /api/events` (`create_event`)**: si no se manda `commercial_staff_id`, y quien crea es `comercial`, se autoasigna; si quien crea es `admin`/`super_admin` (no es comercial), es **obligatorio elegir uno explícitamente** (400 si no). Si se manda un id, se valida que corresponda a una cuenta `comercial` activa.
- **`PATCH /api/events/{id}` (`update_event`)**: reasignar `commercial_staff_id` está permitido para `comercial`/`admin`/`super_admin` — un `coordinador` puede seguir editando el resto del evento (fechas, estado, etc., sin cambios) pero recibe 403 si intenta tocar este campo específico.
- **`GET /api/staff/commercials`** (nuevo, `app/routers/staff.py`, `comercial`+) — mismo patrón que el ya existente `/staff/coordinators`, para poblar el selector al crear/editar un evento y los filtros de `/eventos`.
- **Filtros de pertenencia en `GET /api/events/search`**: gana `commercial_staff_id`/`coordinator_staff_id` (ambos opcionales, combinables entre sí y con `q`/`status`). `templates/eventos.html` — una `comercial` ve un selector "Mis eventos" (default, usa su propio id) / "Todos"; `admin`/`super_admin` ven dos selectores para filtrar por cualquier comercial o coordinador asignado (poblados vía `/staff/commercials`/`/staff/coordinators`). Cada tarjeta de evento ahora también muestra "Comercial: X · Coordinador: Y".
- **`templates/nuevo_evento.html`** gana el campo "Comercial asignada \*" (mismo patrón de datalist que "Coordinador asignado") — si quien crea es `comercial`, aparece preseleccionada a sí misma (editable); `admin`+ tiene que elegir.
- **Alcance de esta fase, avisado explícitamente**: el filtro de pertenencia solo se implementó en Eventos — Calendario (`/calendario`) sigue siendo un placeholder (Fase 6, no iniciada); cuando se construya el módulo real, reusa el mismo `commercial_staff_id`/`coordinator_staff_id` de `GET /api/events/search` (o el endpoint de rango de fechas que se agregue) para el mismo filtro.
- Verificado con `TestClient` (10 casos: comercial se autoasigna, comercial elige a otra comercial, admin sin elegir → 400, admin elige válida → 200, `commercial_staff_id` apuntando a un rol que no es comercial → 400, PATCH reasigna, coordinador no puede reasignar → 403, filtro "mis eventos", filtro admin por coordinador, `/staff/commercials` lista solo comerciales activas) + sweep de render Jinja de `nuevo_evento.html`/`eventos.html` en 4 roles + `node --check` del JS inline de ambos templates (extraído y verificado por separado, ya que no son archivos `.js` sueltos). **No se pudo hacer la pasada en navegador real esta vez** — el panel de navegador integrado rechazó la navegación a `localhost` en este entorno (gate de permisos, no relacionado con el código); queda pendiente una verificación visual en la próxima sesión o por Juan David al probar en local.

## Sprint 2.4 (2026-09-16) — Fase 6: ocultar funciones operativas para "comercial"

Pedido explícito, corrige parte de lo decidido en Fase 0: "la comercial no debe poder imprimir en Directorio en Vivo... bloquea que no aparezca para ella la opción de código QR, Adjuntar BD, Escarapelas, Usuarios de Evento (no bloqueado del todo, solo para crear clientes), Parámetros del Evento — literal que visualmente no salgan". En Fase 0 se documentó que `comercial` hereda automáticamente el acceso operativo de `coordinador`+ (roster, escarapelas, parámetros) porque queda por ENCIMA en `STAFF_ROLES` — esta fase revierte puntualmente esa herencia para 3 de esas funciones, sin tocar el resto de lo que `comercial` sí conserva (estadísticas, reporte, cambio de estado, editar evento/cliente).

- **`app/auth.py`: `require_role_excluding(minimum_role, excluded_roles)`** (nuevo) — mismo comportamiento que `require_role`, pero además rechaza (403) roles puntuales aunque cumplan la jerarquía. Necesario porque `comercial` está por encima de `coordinador`/`digitador` en `STAFF_ROLES`, así que un `require_role("coordinador")` simple no puede excluirlo sin tocar la jerarquía general (que sigue siendo correcta para todo lo demás).
- **Backend, endpoints que ahora excluyen a `comercial` explícitamente** (403, antes pasaban por herencia de jerarquía):
  - `POST /api/bulk_register` (Adjuntar Base de Datos, `app/routers/api.py`).
  - Todos los de `app/routers/badges.py` (Escarapelas — diseño Y también impresión: `GET/PUT badge-template`, `saved-badge-templates` list/save-as/import, `DELETE saved-badge-templates/{id}`, `upload-image`, `badge-print-data`, `photo`, `print-count`, `print-log`). Se excluye tanto el editor (antes `coordinador`+) como la impresión (antes `digitador`+, que `comercial` también superaba) — el pedido fue explícito en que no debe poder ni diseñar ni imprimir.
  - `app/routers/parametros.py` (Parámetros del Evento — `optional-fields`, `field-configs` GET/PUT).
- **Backend, sin cambios a propósito**: `POST /api/events/{id}/staff-users` (Usuarios del Evento) — ya tenía su propia excepción desde Fase 0 (`comercial` puede crear `cliente`, no `digitador`), coincide exactamente con "no bloqueado del todo, solo para crear clientes", así que no hizo falta tocar nada ahí.
- **Páginas (`app/main.py`)**: `_resolve_kiosk_page` ya tenía un parámetro `exclude_roles` (usado antes para excluir `digitador` de Estadísticas) — se reutilizó agregando `exclude_roles=["comercial"]` a `kiosk_roster`, `kiosk_parametros`, `kiosk_badge_editor` y `kiosk_badge_print` (esta última sin `min_role`, pero con `exclude_roles` igual). Entrar por URL directa a cualquiera de estas cuatro redirige a `/kiosk/{event_id}` para `comercial`, aunque el link ya no aparezca.
- **`templates/kiosk_select.html`**: las tarjetas "Adjuntar Base de Datos", "Escarapelas" y "Parámetros del Evento" quedan dentro de un `{% if staff_role != "comercial" %}` — literalmente no se renderizan para esa cuenta. "Usuarios del Evento" y "Estadísticas" quedan fuera de ese bloque, siguen visibles.
- **Frontend, botones de impresión**: `static/js/directory.js` (botón 🖨️ persistente por fila del Directorio en Vivo) y `static/js/app.js: offerPrint` (botón "Imprimir Escarapela" tras un registro exitoso, escáner facial/cédula) ahora comprueban `window.STAFF_ROLE !== 'comercial'` antes de mostrarse — evita ofrecerle un botón que el backend ya le rechazaría con 403.
- Verificado con `TestClient` (22 casos: las 3 páginas bloqueadas dan 302 para comercial, las páginas permitidas (Usuarios del Evento/Estadísticas/Registro) siguen en 200, `kiosk_select.html` oculta las 3 tarjetas y conserva las 2 permitidas, los 7 endpoints de API dan 403 para comercial, y — control — `coordinador` sigue viendo/usando todo exactamente igual que antes, sin ninguna regresión) + `node --check` en los 2 JS tocados. Regresión completa de las fases anteriores del sprint (comercial/checkin/prints/phone/commercial-assignment, 90 casos) sigue en verde.

## Sprint 2.4 (2026-09-16) — Fase 7: reforzar unicidad de cédula (validación DB) + bug real de la Fase 3

Pedido explícito: "también haz la validación desde base de datos y para cuando se crean nuevos o se editan cédulas de que no se puede repetir para nadie el id". Al revisar qué tan cubierto estaba esto, se encontraron:

**Ya estaba cubierto (verificado, no se tocó):**
- **Creación**: `User` usa PK compuesta `(id, tenant_id)` desde el diseño original del proyecto — un `INSERT` con una cédula que ya existe en ese tenant es rechazado por la propia base de datos (`IntegrityError`), no depende de que el código lo valide a mano. `manual_register`/`bulk_register` ya evitan pisar esto por diseño: si la cédula ya existe, reutilizan a la persona (upsert) en vez de intentar crearla de nuevo.
- **Edición**: `PUT /api/users/{user_id}/cedula` (Sprint 2.3) ya consulta si la cédula nueva está libre en ese tenant ANTES de aplicar el cambio, devolviendo 409 si no — patrón seguro insertar-reapuntar-borrar, documentado ahí mismo.

**Reforzado esta fase (`app/routers/api.py`):**
- **`update_user` (`PATCH /api/users/{id}`) ahora descarta explícitamente `id`/`tenant_id` del body** antes del `setattr` genérico — antes, si alguien mandaba `id` en el JSON (por API directa, no por la UI normal), el endpoint intentaba un `UPDATE` crudo sobre la propia llave primaria compuesta, algo para lo que nunca estuvo pensado (el único camino válido y seguro para cambiar una cédula es `PUT .../cedula`). Verificado: mandar `id` en el `PATCH` genérico ahora se ignora silenciosamente (200, sin aplicar el cambio) en vez de arriesgar una inconsistencia.

**Bug real encontrado y arreglado de paso (regresión propia de la Fase 3 de este mismo sprint):** `PUT /api/users/{user_id}/cedula` reapunta las filas hijas de `EventAttendee`/`AccessLog` a la cédula nueva antes de borrar el `User` viejo — pero el modelo `PrintLog` (agregado en la Fase 3, con la MISMA FK compuesta `(user_id, tenant_id)` hacia `users`) nunca se agregó a esa reasignación. Consecuencia real: renombrar la cédula de alguien que ya se había impreso al menos una vez tumbaba el endpoint con un `ForeignKeyViolation` al intentar borrar el `User` viejo (sus `PrintLog` seguían apuntándolo). Se agregó el mismo `UPDATE ... synchronize_session=False` que ya existía para `EventAttendee`/`AccessLog`, ahora también para `PrintLog`. Reproducido y confirmado con `TestClient` (SQLite + `PRAGMA foreign_keys=ON`) antes y después del fix — mismo método que atrapó los bugs de `autoflush` documentados más abajo en este archivo.

Verificado con `TestClient` (10 casos: `id` en el `PATCH` genérico se ignora sin crashear ni aplicar el cambio; `PUT .../cedula` sigue devolviendo 409/400 en los casos ya cubiertos; renombrar a alguien con historial de impresiones y registro ya no truena, y `PrintLog`/`AccessLog`/`EventAttendee` quedan los 3 reapuntados correctamente a la cédula nueva, con el `User` viejo ya eliminado; insertar un `User` duplicado directo contra la base confirma que el `IntegrityError` de la PK compuesta sigue siendo el guardrail de fondo). Regresión completa del sprint (112 casos de fases anteriores) sigue en verde.

## Sprint 2.4 (2026-09-16) — Fase 8: módulo de Calendario

Pedido explícito: reemplaza el placeholder de `/calendario` por un calendario real — vista mensual por defecto (cuadrícula con tarjetas de evento por día), con vista semanal y diaria alternables, filtros de pertenencia (mismos de Fase 5: "Mis eventos"/"Todos" para comercial, filtro por cualquier comercial/coordinador para admin+), clic en un evento para editarlo, y capacidad de dejar recordatorios/anotaciones sobre un día cualquiera.

- **`Event.commercial_staff_id`/`coordinator_staff_id`** (ya existentes) se reusan tal cual para los filtros — no hizo falta ningún campo nuevo en `Event` para esta fase.
- **`CalendarNote`** (nuevo, `app/models.py`, migración `0019_calendar_notes`) — recordatorio libre `(date, text, created_by_id)`, **compartido entre todo el equipo con acceso al Calendario** (coordinador+), no una agenda privada por usuario — pensado como un tablero de anotaciones de equipo ("llamar al cliente X", "confirmar transporte"), a propósito NO atado a ningún evento en particular (para eso ya existe `Event.notes`). Solo su autor o `admin`+ puede borrarlo.
- **`app/routers/calendar.py`** (nuevo router, registrado en `main.py`):
  - `GET /api/events/calendar?start=&end=&commercial_staff_id=&coordinator_staff_id=` — eventos cuyo rango `[start_date, end_date]` se **solapa** con `[start, end]` (no solo los que empiezan ahí — un evento de varios días aparece en cada día que le corresponde), reusando `_serialize()` de `events.py` y los mismos dos filtros opcionales de Fase 5.
  - `GET/POST/DELETE /api/calendar/notes` — listar por rango de fechas, crear (`coordinador`+, texto no vacío), borrar (autor o `admin`+, 403 para cualquier otro).
- **`templates/calendario.html`** (reescrito por completo, antes placeholder):
  - Vista **mensual por defecto** (cuadrícula 6×7, con celdas de meses adyacentes atenuadas), **semanal** y **diaria** alternables con un selector Mes/Semana/Día + navegación `←`/`→`/"Hoy". Cada celda muestra chips de evento (color según `status`, mismo criterio que `eventos.html`) y chips de recordatorio, más un "+ recordatorio" para agregar uno rápido (usa `window.showPrompt`, ya existente en `toast.js`).
  - **Filtros de pertenencia**: idénticos en patrón a `eventos.html` (Fase 5) — `comercial` ve "Mis eventos"/"Todos", `admin`+ ve selectores de comercial/coordinador específicos, poblados vía `/api/staff/commercials`/`/api/staff/coordinators`.
  - **Clic en un evento abre un modal de edición** (nombre, fechas ×3, horas ×4 con `clockpicker.js` reusado de `nuevo_evento.html`, país/ciudad con el mismo datalist de GeoNames, lugar, dirección, coordinador asignado, comercial asignada — este último campo **solo se renderiza para `comercial`/`admin`/`super_admin`**, ya que reasignar la comercial es 403 para `coordinador` desde el backend de Fase 5; para `coordinador` el campo simplemente no aparece en el modal) — guarda vía el mismo `PATCH /api/events/{id}` que ya existía, sin backend nuevo para esto.
  - Borrar un recordatorio pide confirmación (`showConfirm`) y solo ofrece el botón `✕` si `created_by_id` coincide con `STAFF_ID` o el rol es admin+ — mismo criterio que el backend, para no mostrar un botón que fallaría con 403.
- Verificado con `TestClient` (11 casos de backend: solapamiento de rango de fechas con un evento multi-día que cruza de un mes a otro, filtros por comercial, filtro inexistente da vacío, crear/listar/borrar recordatorios, texto vacío rechazado, autor puede borrar su propia nota pero otra cuenta no la ajena) + sweep de render Jinja en 5 roles (`coordinador`/`comercial`/`admin` entran, `digitador`/`cliente` redirigidos) + `node --check` del JS inline renderizado tanto para `admin` (con el campo de comercial en el modal) como para `coordinador` (sin ese campo) — 21 casos en total, todos en verde. **No se pudo hacer la pasada en navegador real** — mismo bloqueo de permisos del panel integrado documentado en la Fase 5 (rechaza navegar a `localhost` en este entorno); la lógica de fechas/render se revisó a mano con cuidado extra por esto, pero conviene una verificación visual real en la próxima sesión o por Juan David al probar en local, sobre todo el layout de la cuadrícula mensual y el comportamiento de arrastre/clic de las tarjetas.

## Sprint 2.4 (2026-09-16) — Fase 9: fuente Agrandir como tipografía por defecto de toda la app

Pedido explícito: aplicar "Agrandir" (fuente comercial, no está en Google Fonts) como fuente por defecto de toda la app, a menos que se cambie en Configuración — "obvio aplica tamaños negrillas y eso normal como ya se hacía". Juan David adjuntó dos archivos: `agrandir-regular.otf` (para títulos/negrillas) y `agrandir-thin.ttf` (para texto normal).

- **`static/fonts/agrandir-thin.ttf`** y **`static/fonts/agrandir-regular.otf`** (nuevos, binarios de la fuente) — carpeta nueva, servidos por el mismo mount de `/static` que ya existe (sin backend nuevo).
- **`static/css/style.css`**: el `@import` de Google Fonts (Playfair Display/Montserrat/Raleway, la fuente por defecto ANTERIOR) se reemplazó por dos `@font-face` locales, **un solo `font-family: 'Agrandir'`** con dos pesos:
  - Thin → `font-weight: 100 500` (rango variable — cualquier peso 100-500 pedido, incluido el 400 normal de texto de cuerpo, cae en este archivo).
  - Regular → `font-weight: 600 900` (cualquier `font-weight:700` ya usado por botones/títulos/`th` de tablas, etc. — sin tocar esos sitios uno por uno — cae en este archivo, más marcado).
  - `--font-heading-primary`/`--font-heading-secondary`/`--font-body` (las 3 variables que controlan toda la tipografía de la app) pasan de `'Playfair Display'`/`'Montserrat'`/`'Raleway'` a `'Agrandir'` los tres — como es una sola familia con dos pesos, no hace falta variar cuál variable usa cuál peso: el navegador ya resuelve solo qué archivo cargar según el `font-weight` que cada elemento ya pedía.
- **`templates/configuracion.html`**: la opción "Original" del selector de tipografía (la que limpia `localStorage` y deja los valores por defecto de `style.css`) cambia su etiqueta de "Original (Playfair + Montserrat + Raleway)" a "Original (Agrandir)" — sigue siendo la opción que representa "sin override", ahora Agrandir es lo que hay detrás de esa opción. El resto del selector (las 40 familias de Google Fonts) sigue exactamente igual — Configuración sigue pudiendo cambiar la fuente por computador sin tocar nada de esto.
- **Alcance, avisado explícitamente**: solo se tocó la tipografía GLOBAL de la app (`style.css` + Configuración). El catálogo de fuentes del editor de escarapelas (`badge_editor.html: FONT_FAMILIES`, las 40 de Google Fonts para diseñar el CONTENIDO IMPRESO de una escarapela) es un catálogo aparte, a propósito — no era parte de este pedido y sigue igual.
- Verificado sirviendo los archivos reales por HTTP (200, tamaño en bytes idéntico al original tanto para el `.ttf` como el `.otf`) y revisando `style.css` servido tal cual queda en el navegador (sin errores de sintaxis CSS). **No se pudo ver el resultado renderizado en un navegador real** — mismo bloqueo de permisos del panel integrado documentado en las Fases 5/8 de este mismo sprint; conviene un vistazo visual en la próxima sesión o por Juan David al probar en local, aunque el mecanismo (`@font-face` con rango de `font-weight`, mismo patrón estándar de cualquier fuente con múltiples pesos) es sencillo y de bajo riesgo.

## Sprint 2.4 (2026-09-17) — Fases 10 a 16: segunda ronda grande de correcciones

18 pedidos en un solo mensaje, tras probar todo lo anterior. Igual que la ronda anterior, se implementó en fases separadas, cada una verificada con `TestClient`/sweep de render/`node --check` y commiteada aparte en `sprint2/reverify-fixes-and-badges` (nunca a `main`). **No se pudo hacer ninguna pasada en navegador real en toda esta ronda** — el panel integrado siguió rechazando conectarse a `localhost` en este entorno (mismo bloqueo reportado desde la Fase 5); toda la verificación fue automatizada.

**Fase 10 — correcciones de UI**: el link a Configuración en `_sidebar.html` estaba oculto para no-admin por error (la página en sí ya era accesible para cualquier rol desde antes) — bug real, corregido. El selector de tipografía en Configuración ahora muestra cada opción **en su propia fuente** (antes había que aplicarla para verla) — necesita `goldenEnsureFontLink()` llamado al poblar el `<select>`, no solo cuando ya hay una fuente guardada. `nuevo_evento.html` precarga País=Colombia/Ciudad=Bogotá (editable). `clientes.html` gana un buscador de eventos DENTRO de la tarjeta de cada cliente (cachea la lista ya cargada, filtra en memoria). "Clientes" se renombra a "**Clientes - Cuentas**" en sidebar/títulos/franjas de contexto en toda la app (NO afecta el rol `cliente` de `STAFF_ROLES`, que es un concepto distinto — se dejó explícitamente intacto).

**Fase 11 — permisos + estado de evento desde Eventos/Calendario**: `DELETE /api/users/{id}` y `PATCH .../status` pasan de admin+ a **coordinador+** (pedido explícito). El botón "Aplicar" separado del cambio de estado se quitó de `directory.js: buildEditModal` — ahora viaja junto con el resto de cambios al pulsar el único "Guardar cambios" (con su propio `showConfirm` solo si el estado realmente cambió). `GET /api/report` deja de usar `require_role_or_client` — cliente ya NO puede exportar (sigue viendo gráficos vía `stats.py`, sin cambios ahí); `kiosk_estadisticas.html` oculta el botón para ese rol. `eventos.html` gana un `<select>` de estado por tarjeta (mismo patrón que `clientes.html`); `calendario.html`'s modal de evento gana selector de estado + botón "🚪 Ingresar al evento" (antes solo editaba, ahora también entra al kiosko).

**Fase 12 — "Color de pañoleta" + contraste automático de texto**: `Event.bandana_color`/`bandana_color_name` (input de color nativo con gotero + nombre libre), editable al crear/editar un evento. Los chips de evento en `calendario.html` se colorean por pañoleta en vez de por estado (gris neutro de respaldo para eventos viejos sin pañoleta). `theme.js: goldenContrastColor(hex)` calcula blanco/negro por luminancia percibida (coeficientes ITU-R BT.601) — se aplica al acento elegido en Configuración (`--golden-primary-contrast`/`--golden-sidebar-contrast`, ya no fijos) y a los chips del calendario; **alcance explícito**: no se tocó cada color hardcodeado del sistema (los `.status-*` fijos no son elegibles por el usuario, no lo necesitan), solo los lugares con un color elegido libremente.

**Fase 13 — dos bugs reales**: (1) la autoimpresión "no servía" — `maybeAutoPrint` llamaba a `openPrintWindow`, que desde la Fase 3 pregunta "¿reimprimir?" si la persona ya se había impreso antes; ese modal nadie lo confirma en un flujo automático, así que la autoimpresión se quedaba esperando en silencio para siempre. Arreglo: la autoimpresión nunca pasa por ese confirm, registra el print-log y abre directo — y se agregó la llamada faltante en `directory.js` (cambiar el estado a "Registrado" desde el modal de Editar también dispara autoimpresión). (2) una imagen en una escarapela no aparecía en la vista previa de impresión del navegador — `waitForImages` en `badge_print.html` solo esperaba el evento `load`, que Chrome puede disparar antes de terminar de decodificar; se agregó `img.decode()`.

**Fase 14 — destinatarios en los recordatorios del Calendario**: `CalendarNote.target_staff_ids` (JSON de ids, vacío = todo el equipo, con ids = solo esas personas + quien lo creó). `GET /api/calendar/notifiable-staff` (excluye cliente/digitador) alimenta un modal nuevo en `calendario.html` con buscador + selección múltiple (lista desplegable y texto libre a la vez) en vez del `showPrompt` de una sola línea de la Fase 8.

**Fase 15 — doble rol coordinador+comercial**: `StaffUser.secondary_role` — el ÚNICO par permitido, en cualquier orden, asignado por admin+ desde `staff.html` (`PATCH /api/staff/{id}/secondary-role`). `app/auth.py` gana `effective_roles(staff)` — `require_role`/`require_role_excluding`/`require_role_or_client` ahora razonan sobre el CONJUNTO de roles (jerarquía = el máximo; exclusión de Fase 6 = solo si TODOS sus roles están excluidos), así que un coordinador+comercial hereda la unión de ambos en vez de quedar atrapado por las restricciones de uno solo. Los checks hardcodeados de `events.py` (`create_event_staff`, reasignar comercial) se migraron al mismo criterio. `main.py: _display_role()` resuelve un problema aparte: decenas de templates Jinja comparan `staff_role` contra un solo string (`{% if staff_role != "comercial" %}`) — reescribir cada uno sería un cambio mucho mayor al pedido, así que se decide UNA sola etiqueta a mostrar (si tiene ambos roles, se muestra como `"coordinador"`, el menos restringido de los dos en toda la UI existente) en el único punto donde se arma el contexto de cada página — la distinción real de permisos ya vive en el backend vía `effective_roles()`.

**Fase 16 — reporte Excel rediseñado por completo**: reemplaza el formato fijo/legacy (`Tipo_Pago`, `usuario`, columnas pensadas para un integrador externo que ya no aplica) por uno dinámico. `AccessLog.registration_method` (`'tradicional'|'autoregistro'|'biometrico'|'qr'`) se marca en cada uno de los 5 puntos donde se crea un log de registro real: `/recognize` siempre `"biometrico"`; `checkin_cedula` exacto → `"autoregistro"` si `auto_register` estaba prendido, si no `"tradicional"`; `manual_register` (alta nueva o reutilizar de otro evento) y el cambio manual de estado en el Directorio → `"tradicional"`; la edición de perfil (`record_type="Actualizado"`) NO se toca — no es una acreditación, y `reports.py` la excluye explícitamente al buscar el "primer registro real" de cada persona (si no, un evento editado antes de llegar mostraría la hora de la edición como si fuera la hora de llegada). `reports.py` ahora: solo incluye columnas con al menos un dato real en ESTE evento (identidad siempre; el resto — teléfono, correo, cargo, opcionales con su nombre real — solo si alguien lo trae), siempre agrega "Tipo de Registro"/"Fecha de Registro"/"Hora de Registro" (con segundos), y aplica formato de Excel real: encabezados con estilo (fondo oscuro, negrilla), autofiltro activo, ancho de columna según contenido, y 3 filas de título arriba de la tabla (Cliente/Cuenta, Evento, Código) — pensado para mandarse directo al cliente. Bug real encontrado y corregido antes de cerrar: un evento sin nadie en el directorio (`raw_rows` vacío) rompía `get_column_letter(0)` porque `pd.DataFrame([])` no tiene columnas sin importar qué calculara el código — se resolvió pasando `columns=` explícito al DataFrame.

Verificado con `TestClient` en las 7 fases (más de 100 aserciones nuevas en total: render sweeps de cada template tocado, `node --check` del JS inline, permisos de cada endpoint nuevo/modificado, y para la Fase 16 en particular una verificación completa abriendo el `.xlsx` generado con `openpyxl` para confirmar columnas dinámicas, etiquetas de `Tipo de Registro`, hora exacta con segundos, y el formato/autofiltro reales) — toda la regresión de fases anteriores del sprint sigue en verde.

## Sprint 2.4 (2026-09-17) — Fase 17: bug real de QA — revertir a "No registrado" no persistía

Reportado en la primera pasada de QA real sobre toda esta ronda (ver `qa_sprint2_4_local.md` de Juan David): cambiar el estado de alguien de "Registrado" a "No registrado" desde el modal de Editar respondía 200 y mostraba "✓ Cambios guardados", pero el estado **no cambiaba** — seguía en "Registrado" tanto en la tabla como en `GET /api/users`.

**Causa real**: `record_type="Actualizado"` (el log que `update_user` crea SIEMPRE después de guardar cualquier campo de perfil, para llevar un rastro de ediciones) se estaba contando IGUAL que `"Nuevo"`/`"Existente"` (acreditaciones reales) en tres lugares que calculan si alguien está "Registrado":
1. `get_all_users` (`GET /api/users`, el Directorio en Vivo).
2. `_already_checked_in` (el guardrail de `DUPLICADO` en `recognize`/`checkin_cedula`/`manual_register`).
3. `_status_of` en `stats.py` (la variable "Estado de registro" de Estadísticas — mismo bug, no reportado por QA pero encontrado de paso al corregir los otros dos).

El bug se volvió VISIBLE recién con la Fase 11 (unificar "Cambiar estado" dentro del mismo botón "Guardar cambios"): antes, cambiar el estado era una acción aparte que no disparaba automáticamente un guardado de perfil; ahora, el mismo clic que revierte el estado a "No registrado" (borra los `AccessLog` reales) SIEMPRE sigue con un `PATCH /api/users/{id}` (guardar el resto del formulario) — y ese segundo paso crea un `AccessLog("Actualizado")` nuevo, que los tres lugares de arriba volvían a contar como si la persona estuviera acreditada, deshaciendo el revert en el mismo clic que lo pedía.

**Fix**: los tres lugares ahora excluyen explícitamente `record_type == "Actualizado"` al decidir si alguien está registrado — ese `record_type` es y siempre fue solo un rastro de auditoría de ediciones de perfil, nunca una prueba de asistencia (mismo criterio que ya se había aplicado en `reports.py` desde la Fase 16, al calcular el "primer registro real" de cada persona para el reporte — ahí sí se había hecho bien desde el principio).

Verificado con `TestClient` reproduciendo EXACTAMENTE la secuencia del bug (acreditar → revertir a no_registrado vía el endpoint de estado → guardar perfil vía el endpoint genérico, igual que hace el modal unificado → confirmar que el estado sigue en "No registrado" y que un check-in real después no dispara un `DUPLICADO` falso por culpa del log "Actualizado" que quedó de por medio) — 6/6 casos, y la regresión completa del sprint (más de 180 aserciones de fases anteriores) sigue en verde.

## Sprint 2.4 (2026-09-16) — Fase 2: rediseño del flujo de acreditar por cédula

Pedido explícito, confirmado con el usuario antes de implementar: escanear cédula debe comportarse **como una búsqueda** — la fila de esa persona aparece filtrada en el Directorio en Vivo con los botones normales (Editar/Imprimir), sin modal flotante aparte.

- **`checkin_cedula` (`app/routers/api.py`)**: un match EXACTO por cédula acredita de una vez, **sin importar `Event.auto_register`** — ya no devuelve `MATCH_PENDING` para cédula (sigue existiendo para `/recognize`, facial, sin cambios — un embedding facial sí puede dar falsos positivos, una cédula exacta no). Si NO hay match exacto, ya **no se ofrece alta manual automática** — en su lugar, si el frontend mandó un nombre (`first_name`/`last_name`, viene del CSV de la cédula vieja o del OCR de la MRZ nueva), se intenta un fallback por nombre entre TODOS los `User` del tenant (`_matches_by_word_prefix`, importado de `app/routers/events.py` — mismo criterio que ya usa `/api/events/search`) y se devuelven como `name_matches` — sin acreditar a ninguno solo. Nuevo `result: "NO_MATCH"` reemplaza al viejo `"NO"` para este caso.
- **`static/js/directory.js: fastCheckin`**: en `SÍ`, recarga el directorio y deja la fila filtrada por esa cédula (`cedulaInput.value = data.data.id; applyFilters()`). En `NO_MATCH` con nombre disponible, limpia el campo de cédula y llena el de nombre con el fallback, filtrando el Directorio a las coincidencias — el operador decide desde ahí (ya no hay auto-apertura del alta manual). Se eliminaron `showMatchConfirmModal` (dead code, ya no se llama desde ningún lado) y `goToManualWithCedula`/`opts.onNotFound` (`kiosk_registro.html`).
- **"Registrar con cédula" dentro de "Registrar nuevo"** (`kiosk_registro.html`) — sección nueva arriba del formulario: un campo de escaneo (cédula vieja, Enter) + "📎/📸 Cédula nueva" (foto + OCR MRZ, mismo endpoint `cedula-mrz-scan`). Llena ID/Nombres/Apellidos del formulario de alta, sin buscar/acreditar nada — el operador completa el resto y guarda como siempre. `GoldenDirectory.parseOldCedulaBarcode` se expuso públicamente para reusar el mismo parser CSV.
- El switch "Modo autoregistro" queda **exclusivo de `/recognize` (facial)** — ya no tiene efecto sobre cédula (siempre acredita directo en match exacto).
- Verificado con `TestClient` (6 casos, incluye el caso real reportado: "Sebastian Angarita" en la base, escaneo con nombre "Sebas Angarita" y cédula equivocada encuentra el match correcto; "Ju Perez" NO genera falsos positivos) + sweep de render + `node --check` + navegador real (match exacto acredita y filtra sin modal; fallback por nombre con cédula mal escrita encuentra a la persona correcta filtrando el Directorio; "Registrar con cédula" llena el formulario de alta).

## Sprint 2.3 (2026-09-16) — Menú lateral, cédula editable, opcionales desde Parámetros, fix real de Tesseract

Ronda de feedback tras entregar Parámetros del Evento. Siete pedidos + un bug real encontrado de paso.

**Bug real encontrado (no reportado, arreglado de una):** `kiosk_entry` (`/kiosk/{event_id}`, `app/main.py`) renderizaba `kiosk_registro.html` con un `TemplateResponse` manual para `cliente`, sin pasar por `_resolve_kiosk_page` — le faltaban `field_configs_json`/`optional_labels_json` (agregados con Parámetros del Evento), lo que rompía el `<script>` entero de esa página (JS inválido, `window.FIELD_CONFIGS = ;`) para cualquier `cliente` que llegara ahí vía el auto-redirect desde "/" con un solo evento autorizado. Se arregló haciendo que esa rama use `_resolve_kiosk_page` igual que el resto, eliminando el código duplicado que había quedado desactualizado.

**Fix real de Tesseract** (`.github/workflows/deploy.yml`) — el 503 de "Escanear foto de cédula nueva" seguía reportándose una y otra vez porque el pendiente ("instalar Tesseract en la VM") vivía solo como nota en este archivo, nunca se automatizó. Se agregó un paso `sudo apt-get install -y tesseract-ocr` al workflow de deploy (idempotente) — el próximo push a `main` lo instala solo, sin intervención manual. No se puede probar en este sandbox (no hay VM real); se confirma en el primer deploy real a producción.

**Parámetros del Evento: agregar opcional + reflejarlo en el reporte**:
- `POST /api/events/{event_id}/optional-fields` (nuevo, `app/routers/parametros.py`, coordinador+) — body `{"label": "..."}`, busca el primer slot `opcional_N` libre (1..30) y lo rotula directo en `Event.optional_field_labels`, sin pasar por un alta/carga de roster. Botón "+ Agregar campo opcional" nuevo en `kiosk_parametros.html`, agrega la fila a la tabla sin recargar.
- `app/reports.py: ReportManager.generate_excel_report` — **corrección real de scoping**: antes filtraba por `tenant_id` (mezclaba en un mismo reporte a personas de TODOS los eventos del cliente); ahora recibe `event_id` y usa el mismo universo que Directorio/Estadísticas (`EventAttendee ∪ AccessLog` de ese evento — réplica local de `_event_directory_users`, ver comentario en el archivo sobre por qué no se importa desde `routers/stats.py`). Necesario para que las columnas de opcionales tengan sentido: el mismo `opcional_1` puede rotularse distinto en dos eventos del mismo cliente. Agrega una columna por cada `opcional_N` rotulado, con el rótulo real como encabezado.
- Verificado con `TestClient`: el reporte de un evento no trae personas de otro evento del mismo tenant; la columna nueva aparece con el valor correcto. Verificado en vivo: agregar "Alergias" desde Parámetros y descargar el reporte trae la columna.

**Cédula editable** (`PUT /api/users/{user_id}/cedula`, nuevo, `app/routers/api.py`, **admin+ solamente**) — `User.id` es cédula Y parte de la PK compuesta (`id`, `tenant_id`), referenciada por FK desde `EventAttendee`/`AccessLog`; renombrarla in-place violaría esa FK a mitad de camino. Patrón seguro usado: insertar un `User` nuevo con el id correcto (copiando el resto de columnas, incluido `face_encoding`), reapuntar en bloque (`UPDATE ... synchronize_session=False`) las filas de `EventAttendee`/`AccessLog` de la cédula vieja a la nueva, borrar el `User` viejo, y renombrar la foto física si existía — todo en una transacción. admin+ porque `User` se comparte entre eventos del mismo tenant (ver "decisión de modelado" en Arquitectura): corregir la cédula afecta a esa persona en TODOS los eventos de ese cliente, no solo en el actual. Frontend: `static/js/directory.js: buildEditModal` — el campo "Cédula" deja de estar `disabled`, gana su propia mini-sección "Corregir cédula" + botón "Aplicar" (mismo patrón que "Estado de registro"/"Eliminar" en ese modal: confirmación propia, solo visible para admin+). Verificado con `TestClient` (rename exitoso reapunta `EventAttendee`/`AccessLog` y conserva `extra_fields`; `new_id` duplicado → 409; igual al actual → 400; coordinador no-admin → 403) y en vivo en navegador (la persona sigue apareciendo en el Directorio con la cédula nueva, ya no con la vieja).

**Pestaña "Registro" sin numerar cuando es la única** (`kiosk_registro.html`) — cuando el evento NO tiene biometría (`facial_enabled=False`), solo existe esa pestaña; ahora dice "Registro" a secas en vez de "1. Registro". Cuando sí hay dos pestañas (biometría activa), se mantiene "1. Escáner de Acceso" / "2. Registro" — ahí la numeración sigue ayudando a orientarse.

**Menú lateral fijo** (Clientes / Eventos / Calendario / Configuración) — pedido explícito: reemplaza los botones de "volver" (`← Métodos`/`← Panel`, y el link suelto "Staff" en varios headers) que existían en 8 templates distintos. Visible para `coordinador`/`admin`/`super_admin` en TODA la app; **no** para `digitador`/`cliente` (cuentas temporales, sin panel — nunca tuvieron ese link tampoco).
- `templates/_sidebar.html` (nuevo, partial) — `{% include %}`-ado justo después de `<body class="has-sidebar">` en cada template afectado (patrón repetido, no un sistema de layouts nuevo). `sidebar_active` (contexto por página) resalta el link correspondiente; las páginas `/kiosk/...` pasan `"eventos"` (punto de entrada más cercano). **Ojo con este archivo**: como es un partial que se auto-referencia en su propio comentario de documentación, escribir literalmente la sintaxis Jinja `{% include %}` dentro de un comentario HTML causa una **recursión infinita** (Jinja no distingue comentarios HTML de código real) — bug real encontrado en este mismo sprint al escribir el propio comentario del archivo, no usar esa sintaxis literal en comentarios de `_sidebar.html` ni de ningún partial que se incluya a sí mismo por nombre.
- CSS nuevo en `static/css/style.css`: `.golden-sidebar` fija a la izquierda (220px), el header se corre a la derecha (`body.has-sidebar .golden-header { left: 220px; }`) en vez de quedar tapado. Bajo 900px colapsa a un cajón off-canvas con botón `☰` (`.golden-sidebar-toggle`) + backdrop, para no romper el trabajo responsive de la Segunda ronda de feedback.
- **Páginas nuevas** (todas coordinador+, `/configuracion` admin+): `/clientes` (`templates/clientes.html`, migra el árbol Cliente→Eventos que antes vivía en `dashboard.html`, con búsqueda que ahora filtra SOLO por nombre de cliente, en memoria sobre lo que ya trajo `GET /api/tenants`), `/eventos` (`templates/eventos.html`, lista plana con búsqueda nombre/código + filtro de estado — `GET /api/events/search` ganó un parámetro `status` opcional y `q` pasó a ser opcional también, para poder filtrar solo por estado sin texto), `/calendario` (`templates/calendario.html`, placeholder explícito, sin funcionalidad todavía — se define después), `/configuracion` (`templates/configuracion.html`, dos pestañas: "Apariencia" y "Staff y Permisos" — esta última incrusta `/admin/staff?embed=1` en un `<iframe>`, mismo patrón `?embed=1` que ya usaba Estadísticas dentro de la pestaña de `cliente`).
- **`dashboard.html` se simplificó**: ahora solo renderiza para `digitador`/`cliente` ("Tus eventos"). `/` (`app/main.py: dashboard`) redirige a `coordinador`/`admin`/`super_admin` directo a `/clientes` — el árbol combinado de antes se partió en las secciones del menú.
- **Apariencia es una preferencia por computador/navegador, NO por cuenta ni sincronizada** (aclarado explícitamente por Juan David) — `static/js/theme.js` (nuevo, cargado en todos los templates) lee `golden_theme_primary`/`golden_theme_fonts` de `localStorage` y los aplica como overrides de `:root` (`--golden-primary` y las 3 variables de fuente) antes de que se note el flash del tema por defecto. `configuracion.html` ofrece un selector de color (dorado de acento) + 4 combinaciones de tipografía predefinidas (con fallback a fuentes de sistema, no depende de cargar fuentes nuevas). Nunca toca la base de datos — si el usuario abre la app en otro computador, ve el tema por defecto.
- Verificado con `TestClient`: sweep de render de las 4 páginas nuevas + todos los templates con sidebar, en los 5 roles de staff (29 casos, sin errores de Jinja). Verificado en vivo en navegador: el sidebar aparece/resalta correctamente para coordinador+ y está ausente para digitador (confirmado por HTML, no solo visualmente); el cajón off-canvas abre/cierra bien en viewport móvil (400px); cambiar el color en Configuración se aplica al instante y persiste al navegar a otra página (mismo navegador); la pestaña "Staff y Permisos" embebe `/admin/staff` sin duplicar header; búsqueda de Clientes (por nombre) y de Eventos (texto + filtro de estado) filtran correctamente.

**Ronda 2 de feedback sobre Sprint 2.3 (mismo día), tras QA en local:**
- **El color de Apariencia ahora también pinta el menú lateral** (pedido explícito: "el menú siempre está azul oscuro... mejor dejar las letras siempre blancas y que cambie el color del menú") — nueva variable `--golden-sidebar-bg` (`static/css/style.css`, por defecto `var(--golden-dark)`, o sea el mismo look de antes hasta que alguien la personalice). El mismo color que ya elegía el usuario para `--golden-primary` ahora también se aplica a `--golden-sidebar-bg` (`static/js/theme.js` + `configuracion.html`). El texto del menú (`.golden-sidebar a`) se dejó **siempre blanco**, ya no cambia a `--golden-primary` en el link activo — el estado activo se marca con borde izquierdo blanco + fondo translúcido blanco, no con color de texto, para que se siga leyendo bien sea cual sea el color elegido.
- **Bug real corregido en el reporte Excel** (`app/reports.py`) — la columna que corresponde a `opt_1` se llamaba **"Tipo de Empresa"**, un nombre que quedó desactualizado desde antes de que ese campo se renombrara (ver `models.py`) a "Tipo de Asistente" en el resto de la app (formulario, Parámetros, Estadísticas). Encontrado por QA en esta ronda, corregido: la columna ahora se llama "Tipo de Asistente", coherente con el resto.
- **Investigado (no reproducido) un reporte de QA**: un `cliente` recién asignado a un evento veía "No tienes eventos autorizados todavía" aunque la asignación (`EventStaffAuthorization`) sí existía. Se probó el flujo real completo (crear cuenta `cliente` vía `POST /api/events/{id}/staff-users`, login real, `GET /api/my-events`) con `TestClient` y **no se reprodujo** — la asignación y el filtro funcionan igual para `digitador` y `cliente` (ambos ya estaban incluidos en el mismo `if staff.role in ("digitador", "cliente")` desde antes). La hipótesis más probable: el evento asignado no estaba "En Proceso" todavía (los eventos temporales solo ven eventos activos, por diseño) y el mensaje no distinguía ese caso de "no tienes ninguna asignación". Se mejoró de todas formas: `GET /api/my-events` ahora devuelve `{"events": [...], "has_any_authorization": bool}` en vez de un array suelto, y `dashboard.html` usa ese campo para mostrar el mensaje correcto en cada caso. Si el síntoma persiste después de este cambio con un evento confirmado "En Proceso", es un bug real distinto que hay que investigar con acceso a los datos reales (no se pudo reproducir con datos sintéticos).

## Sprint 2 (2026-09-15) — Lector de cédula (Épico M1-7, Parte 2 del brief)

Antes en stand-by (sin muestras reales); se activó el mismo sprint al llegar muestras confirmadas de ambos tipos de cédula colombiana. El campo de "cédula" del Directorio en Vivo (`#searchCedula`, ya funcionaba como "teclado" con cualquier lector desde Historia 1.2) ahora interpreta lo que llega según el tipo de documento, en vez de asumir siempre un ID suelto.

### 2.1 — Cédula vieja (código de barras): CSV con nombre completo
El lector de la cédula vieja entrega un CSV plano, no solo el número: `cedula,nombre1,nombre2,apellido1,apellido2,fecha_nacimiento(AAAAMMDD)` (muestra real confirmada: `1016100329,JHOAN,SEBASTIAN,ANGARITA,ROJAS,19980206`).
- `static/js/directory.js: parseOldCedulaBarcode(raw)` detecta este formato (tiene comas, el primer segmento es solo dígitos, ≥5 columnas) y separa cédula/nombres/apellidos; si el texto no calza con el patrón, se sigue tratando como un ID suelto (comportamiento de siempre, no cambia).
- **Búsqueda en dos pasos**, dentro de `mountSearch()`'s `fastCheckin(cedula, force, nameInfo)`: primero coincidencia exacta por cédula vía `POST /api/checkin-cedula` (sin cambios en el backend); si no hay match Y el escaneo trajo nombre, se busca ese nombre completo entre los usuarios YA CARGADOS en el Directorio (`allUsers`), reutilizando `matchesWordPrefix`/`stripAccents` — el mismo criterio de prefijo por palabra + insensible a tildes del Fix 1 de la Parte 0. Si ninguno de los dos encuentra nada, se cae al comportamiento de siempre: saltar a "Registro Individual" con la cédula (y ahora también nombres/apellidos, si se extrajeron) precargados — `goToManualWithCedula(cedula, nameInfo)` en `kiosk_registro.html`.
- Todo esto es 100% frontend — no hizo falta ningún endpoint nuevo, reutiliza `checkin-cedula` tal cual ya existía.

### 2.2 — Cédula nueva: el QR no se intenta leer + blindaje contra atajos del navegador
El QR de la cédula nueva está **encriptado por la Registraduría Nacional** — no es un QR estándar, y no es viable decodificarlo sin integración oficial de pago. **No se intenta.** Además, en QA se confirmó que ese QR mal leído puede disparar atajos del navegador (una pestaña de búsqueda se abrió sola) porque el lector se comporta como teclado. Mitigación, aplicada en general (no solo para este caso puntual) en los dos campos donde se puede escanear directo — `#searchCedula` en `directory.js` y `#manualIdField` en `kiosk_registro.html`: en `keydown`, si `e.ctrlKey || e.altKey || e.metaKey`, se llama `preventDefault()`/`stopPropagation()` — un lector legítimo nunca necesita una tecla modificadora para escribir texto + Enter.

### 2.3 — Cédula nueva: lectura por FOTO de la zona MRZ (módulo nuevo)
La cédula nueva trae en el reverso una **zona MRZ** (Machine Readable Zone, ICAO 9303, formato TD1 — igual que un pasaporte): 3 líneas de texto de ancho fijo pensadas para OCR, no para lector de barras/QR. El módulo correcto es una FOTO del reverso + OCR, no un escaneo.
- **`app/mrz_parser.py`** — `parse_mrz_td1(line1, line2, line3)`, función PURA (solo texto, sin imágenes ni OCR) para poder probarla sin depender de Tesseract ni de una foto real. Usa la librería `mrz` (PyPI) en vez de reinventar el parseo TD1 y el checksum ICAO a mano — decisión de Juan David tras confirmar que la librería reproduce EXACTO los datos reales del brief (cédula `1013259208`, nombres "JUAN DAVID", apellidos "RAMIREZ JUZGA") y además detecta un dígito verificador corrupto (`checker.fields()` es un namedtuple; `bool(checker)` es la validación de checksum completa). El número de cédula real vive en `optional_data_2` de la línea 2 (no en la línea 1, que trae el número de documento físico de la tarjeta — un dato distinto, ver el brief). Antes de aceptar la lectura, `valid` exige que el checksum ICAO cuadre Y que se haya extraído una cédula no vacía — si no, el endpoint le pide al operador repetir la foto en vez de acreditar con un dato mal leído.
- **`app/mrz_ocr.py`** — `extract_mrz_lines(image_array)`, el único punto que de verdad necesita el binario de **Tesseract instalado en el sistema** (no viene con `pip install pytesseract` — esa librería solo es el "puente" de Python hacia el binario). Preprocesa con Pillow (escala de grises + umbral fijo, sin sumar OpenCV) y restringe el alfabeto reconocido a `A-Z0-9<` (el único que existe en un MRZ real) para mejorar precisión.
- **`app/routers/cedula.py`** — `POST /api/events/{event_id}/cedula-mrz-scan` (mínimo `digitador`+, mismo nivel que `checkin-cedula`/imprimir escarapela): recibe una foto, la pasa por `extract_mrz_lines` + `parse_mrz_td1`, y devuelve `{id, first_name, last_name}` o un 422 pidiendo repetir la foto (MRZ no detectada, o checksum inválido). Si Tesseract no está instalado en el servidor, devuelve 503 con un mensaje claro en vez de un 500 críptico — capturado explícitamente porque es un problema de infraestructura, no de la foto.
- **Frontend** (`kiosk_registro.html`): botón "📷 Escanear foto" junto al buscador de cédula del Directorio, usa `<input type="file" accept="image/*" capture="environment">` (abre la cámara trasera del dispositivo directo, sin tener que armar un `<video>`/`<canvas>` propios como el escáner facial) — al subir la foto, llama al endpoint y pasa el resultado a `window.directorySearch.submitScannedCedula(id, {nombres, apellidos})`, un nuevo método expuesto por `mountSearch()` que reutiliza EXACTO el mismo flujo de dos pasos + `DUPLICADO`/`force` que 2.1, sin duplicar lógica.
- **Dato guardado, no persistido:** `birth_date`/`sex`/`expiry_date` quedan disponibles en el resultado del parser (gratis, ya vienen del mismo parseo) pero no se guardan en ningún lado — no hay columna para eso en `User` hoy y el brief no lo pidió como requisito, solo como "por si se quieren guardar o mostrar también".

### Requisito de infraestructura nuevo: Tesseract OCR
`requirements.txt` ganó `mrz` y `pytesseract` — pero **`pytesseract` NO instala el motor de OCR en sí**, solo llama al binario `tesseract` que debe existir en el sistema:
- **VM de producción (Ubuntu):** `sudo apt install tesseract-ocr` — pendiente de ejecutar antes de que esta función funcione en producción (agregado al backlog de despliegue).
- **Desarrollo local (Windows):** instalar el build de Tesseract de UB Mannheim (`https://github.com/UB-Mannheim/tesseract/wiki`) y asegurarse de que `tesseract.exe` quede en el `PATH`, o configurar `pytesseract.pytesseract.tesseract_cmd` con la ruta exacta si no se agrega al PATH durante la instalación.
- Sin el binario instalado, el endpoint responde 503 con un mensaje claro (no un 500) — así que el resto de la app sigue funcionando igual aunque esta función puntual no esté lista todavía en un entorno dado.
- **Confirmado 2026-09-16, reportado en la segunda ronda de feedback como si fuera un bug:** el 503 al usar "Escanear cédula (foto/cámara)" en un entorno donde Tesseract todavía no está instalado es el comportamiento ESPERADO documentado arriba, no un bug nuevo — falta el paso de instalación de este apartado antes de poder probar ese flujo de punta a punta en ese entorno.

### Cómo probar localmente
El parser (`app/mrz_parser.py`) se puede probar SIN Tesseract instalado — es texto puro. El flujo completo (foto → OCR → parseo) sí necesita el binario instalado (ver arriba) y una foto real del reverso de una cédula nueva. Ver `TESTING.md` sección 13 para el checklist completo.

## Bugs encontrados en testing manual (`TESTING.md`) y arreglados (2026-09-21)

Ejecución completa del checklist de `TESTING.md` (109/118 PASS de entrada, ver `resultados_testing_golden.md` del usuario) — 4 hallazgos reales, los 4 corregidos:

1. **[CRÍTICO] `POST /api/register` (alta manual) sin archivo → 500, no CEDULA-08's `{"error": "No se detectó..."}`.** Causa original: `file: UploadFile = File(None)` — un `<input type="file">` vacío igual manda una parte multipart con `filename=""`, así que `file is not None` da `True` aunque no se adjuntó nada; `await file.read()` devuelve `b""`, y `Image.open(io.BytesIO(b""))` (en `BiometricEngine.process_image_stream`) truena con `UnidentifiedImageError` sin capturar → 500. Mismo patrón que YA se manejaba bien en `bulk_register` para `zip_file` (`if zip_file is not None and zip_file.filename:`) pero no se replicó aquí. Arreglo: `if file is not None and file.filename:`.
   - **Corrección real del mismo bug, 2026-09-23 — el fix de arriba estaba incompleto.** La ronda 2 de testing manual (`resultados_testing_golden_v2.md`) volvió a reproducir el 500 en el MISMO caso ("alta sin foto"), y encima confirmó que "con foto" tampoco probaba nada útil (una foto inválida devuelve el error `{"error": "No se detectó..."}` ANTES de llegar a crear el `User`, así que ese "control" nunca había ejercido el código de creación real). El fix del `file` sí era correcto — el problema real estaba UNA LÍNEA MÁS ABAJO, en el bloque que crea el `User` nuevo: exactamente el mismo bug ya encontrado y arreglado en `bulk_register` (ver el bug de `EVENT_ATTENDEES`/`autoflush=False` más abajo en este mismo historial), pero **nunca se replicó en `manual_register`** — `db.add(user)` seguido de `db.add(log)` + `_upsert_attendee(...)` + un solo `db.commit()` al final, sin ningún `db.flush()` entre medio. Con `autoflush=False` (`database.py`), el INSERT de `users` quedaba pendiente sin mandarse a la base cuando el INSERT de `event_attendees` (que depende de él por llave foránea) se intentaba en el mismo flush → `ForeignKeyViolation`. **Por qué mi verificación anterior no lo agarró:** probé la condición del `if file...` de forma aislada (correcta) pero nunca ejecuté el flujo completo de creación de usuario contra una base con las llaves foráneas realmente activas. Se reprodujo esta vez con un test end-to-end de verdad: FastAPI `TestClient` contra la app real (dependencias de sesión/DB sobreescritas) + SQLite con `PRAGMA foreign_keys=ON` (que sí hace cumplir las FK, a diferencia del SQLite por defecto) — sin la pragma el bug NO se reproducía, lo cual explica por qué mis pruebas de la ronda anterior (con un simulador más simple, sin esta pragma) no lo habían agarrado. Arreglo: `db.flush()` justo después de `db.add(user)`, antes de crear `AccessLog`/`EventAttendee`. **Lección:** cualquier ruta que haga `db.add(User(...))` seguido de una fila dependiente (`AccessLog`, `EventAttendee`) en el mismo `commit()` necesita este `flush()` — si se agrega otro endpoint así en el futuro, replicarlo ahí también (ya son 2 lugares: `bulk_register` por fila con SAVEPOINT completo, `manual_register` con un `flush()` simple porque es una sola fila, no un loop).
2. **[ALTO] `DELETE /api/events/{id}` con historial real → 500 (`ForeignKeyViolation`), pese a que `CLAUDE.md` ya documentaba un fix anterior para esto.** Causa: ese fix (2026-09-18) soltaba `EventStaffAuthorization` y ponía `AccessLog.event_id = NULL`, pero `EventAttendee` se agregó DESPUÉS (migración `0008`, 2026-09-19) con `event_id` **`NOT NULL`** y nunca se actualizó `delete_event` para tenerlo en cuenta — cualquier evento con al menos una persona precargada/registrada (lo cual crea un `EventAttendee` vía `_upsert_attendee`) no se podía borrar. Arreglo en `routers/events.py: delete_event`: `db.query(EventAttendee).filter(EventAttendee.event_id == event_id).delete()` antes del `db.delete(event)` — a diferencia de `AccessLog` (historial real, se conserva con `event_id=NULL`), `EventAttendee` no tiene sentido sin el evento, así que se borra directo (y no se puede dejar en NULL, la columna no lo permite). Verificado con una simulación local (SQLite + `PRAGMA foreign_keys=ON`) reproduciendo el escenario exacto (evento con `AccessLog` + `EventStaffAuthorization` + `EventAttendee`).
3. **[BAJO] `GET /kiosk/{event_id}/roster` no exigía rol mínimo** — un `digitador` podía abrir la página completa por URL directa (200), aunque el `POST /api/bulk_register` real sí seguía protegido (403) — no explotable para escribir datos, pero inconsistente. Arreglo: `_resolve_kiosk_page()` (`main.py`) gana un parámetro opcional `min_role`, chequeado contra `ROLE_HIERARCHY`; `kiosk_roster` ahora lo pasa como `"coordinador"` (mismo mínimo que ya exigía el POST), redirige a `/kiosk/{event_id}` si no alcanza.
4. **[BAJO/UX] El botón "Editar" del Directorio se mostraba para `digitador`/`cliente`** aunque el `PATCH /api/users/{id}` real les diera 403 (el botón "Eliminar" sí se ocultaba bien, porque solo aparece al entrar en modo edición). Arreglo en `directory.js`: se agregan `EDIT_ROLES = ['coordinador','admin','super_admin']` y `DELETE_ROLES = ['admin','super_admin']` (mismos mínimos que exige el backend), comparados contra un nuevo `window.STAFF_ROLE` (inyectado igual que `window.EVENT_ID` en cada template que usa el directorio) — si el rol actual no puede editar, la celda de acción para esa fila queda vacía desde el principio (no se crea el botón); si puede editar pero no borrar, "Eliminar" nunca se revela ni entrando en modo edición.
5. **[BAJO/UX, encontrado en la ronda de pruebas en PRODUCCIÓN, 2026-09-15] El botón "+ Agregar campo opcional" cambiaba de texto a "Guardar Perfil Biométrico" después de un alta manual exitosa.** Causa: `regForm.onsubmit` (`app.js`) resolvía el botón a deshabilitar/reetiquetar con `e.target.querySelector('button')` — un selector genérico que agarra el PRIMER `<button>` del formulario en orden del DOM. Desde que "Registro Individual" tiene el botón "+ Agregar campo opcional" (`type="button"`, ver arriba), ese botón quedó ANTES del submit real en el HTML — así que el selector apuntaba al botón equivocado: el submit real nunca se tocaba, y "+ Agregar campo opcional" terminaba heredando "Guardando..." y después el texto final. Arreglo: `e.target.querySelector('button[type="submit"]')`, y de paso se guarda el label original del botón (`originalLabel`) para restaurarlo al terminar, en vez de un `"Guardar Perfil Biométrico"` quemado que tampoco era correcto cuando `facial_enabled=False` (ese caso dice solo "Guardar Perfil", sin "Biométrico" — ver el template). **Ninguna ronda de pruebas en local lo había encontrado** porque casi todas las pruebas anteriores del flujo de alta manual usaban `fetch` directo o dispatch sintético del evento `submit`, no un clic real en el botón — el mismo patrón de selector genérico se revisó en el resto del código (`liveEditForm` en `app.js`, `rosterForm` en `kiosk_roster.html`) y ahí SÍ es seguro porque esos formularios solo tienen un botón cada uno.

**Notificaciones (2026-09-17):** `static/js/toast.js` reemplaza `alert()`/`confirm()` en todo el proyecto (`dashboard.html`, `kiosk_registro.html`, `staff.html`, `app.js`) — `showToast(mensaje, "success"|"error")` (pill que aparece arriba a la derecha y se desvanece solo, sin bloquear) y `showConfirm(mensaje)` (modal no-bloqueante que devuelve una `Promise<boolean>`, hay que usarlo con `await` dentro de una función `async`). No queda ningún `alert`/`confirm` nativo del navegador en el código — si se agrega un flujo CRUD nuevo, usar estos dos en vez del nativo.

### Modelo de datos
- `Tenant(id, client_code, name, contact_name, contact_phone, contact_email)` — un **cliente** de Golden (empresa para la que se hace el evento), con su contacto. `id` sigue siendo el slug del nombre (interno); `client_code` es un código corto generado al azar y único, pensado para referenciar/asociar el cliente sin exponer el slug (ver sección de códigos únicos abajo). No confundir con `StaffUser` (cuentas de staff interno).
- `User(id, tenant_id)` — la **identidad** de una persona dentro de un tenant (nombre, empresa, teléfono, correo, y opcionalmente `face_encoding` si alguna vez se le tomó foto para reconocimiento facial). Clave primaria compuesta `(id=cédula, tenant_id)`. **Vive a nivel de tenant, no de evento** — la misma persona puede asistir a varios eventos del mismo cliente sin volver a cargar sus datos ni su foto.
- `EventAttendee(id, event_id, user_id, tenant_id, created_at)` — **decisión de modelado (2026-09-19, ver `docs/05_MODELO_DATOS.md` §3 pregunta 1, resuelta con opción (b)):** la lista de personas esperadas/asociadas a UN evento puntual, separada de `User`. Se eligió esta opción (en vez de reusar `User` directamente como "la lista del evento") porque el pedido explícito fue que "cargar la base de un evento sirva para cualquier tipo de registro, no una base por cada tipo" — `EventAttendee` es agnóstico al método (cédula, facial, QR futuro); solo `User.face_encoding` es específico de facial. Una fila aquí significa "esta persona se espera/se reconoce para este evento", independiente de si ya se presentó (eso lo dice `AccessLog`, filtrado por `event_id`). Se crea al cargar el roster (`POST /api/bulk_register` — mismo endpoint de siempre, no se renombró) y también se crea sobre la marcha (upsert) cuando alguien se registra/reconoce en un evento sin haber estado precargado — así el directorio de ESE evento siempre incluye a todos los que efectivamente participaron, se hayan precargado o no.
- `AccessLog(id, tenant_id, user_id, timestamp, record_type, event_id, registered_by_staff_id)` — bitácora de reconocimiento/registro ("Nuevo", "Existente", "Actualizado"). `event_id` y `registered_by_staff_id` se llenan desde `routers/api.py` en cada acción. **El directorio de un evento (`GET /api/users?event_id=`) ahora se arma desde `EventAttendee ∪ AccessLog` filtrados por `event_id`** (antes usaba todos los `User` del tenant sin distinguir evento — con multi-evento por tenant eso mezclaba historial de eventos distintos en el mismo directorio, quedó corregido).
- `StaffUser(id, username, password_hash, full_name, role, tenant_id, is_active, created_by_id)` — cuenta de staff interno. `role` es uno de `STAFF_ROLES` en `models.py`. Para digitadores, `username` es la cédula.
- `Event(id, tenant_id, event_code, name, location, address, country, city, start_date, end_date, setup_date, event_time_start/end, setup_time_start/end, coordinator_staff_id, notes, status, created_by_id)` — una sesión de registro puntual dentro de un tenant. `start_date`/`end_date`/`setup_date` son **`Date`, no `DateTime`** (migración `0005`) — la hora vive por separado en los 4 campos `*_time_*` (`"HH:MM"`), pedirla dos veces en el mismo campo no tenía sentido. `coordinator_staff_id` apunta a un `StaffUser` con `role='coordinador'` (validado en `routers/events.py`). Todos los campos son obligatorios al crear un evento (`EventIn` en `routers/events.py`) salvo `notes`.
  - **Ciclo de vida de `status` (2026-09-18/19, migración `0006`, `EVENT_STATUSES` en `models.py`):** `creado` (default al crear) / `en_proceso` (único estado en el que `digitador`/`cliente` pueden entrar a `/kiosk/{id}`, ver `get_event_for_staff`) / `finalizado`. **Navegación libre entre los tres (2026-09-19):** cualquier `coordinador`+ puede pasar el evento a cualquiera de los tres estados en cualquier momento (ya no es solo hacia adelante) — en el dashboard es un `<select>` por evento en vez de un botón contextual, y cada cambio pide `showConfirm` antes de aplicarse (revierte el `<select>` a su valor anterior si se cancela). `PATCH /api/events/{id}` valida que `status` sea uno de los tres. Eliminar un evento (`DELETE`, admin+) muestra un `showConfirm` mencionando el estado actual antes de borrar.
  - **Dos gates distintos, no confundir (2026-09-18):** `get_event_for_staff` (acceso general al evento/página) solo restringe por estado a `digitador`/`cliente` — `coordinador`+ siempre puede ENTRAR a `/kiosk/{id}` y `/kiosk/{id}/registro` sin importar el estado, porque necesita ver el Directorio y gestionar "Usuarios del Evento" aunque el evento todavía no haya empezado o ya haya cerrado. `require_event_in_progress` (en `auth.py`) es un segundo gate, más estricto, que exige `status == "en_proceso"` **para TODOS los roles sin excepción** (incluido admin/super_admin) — se aplica únicamente a `POST /api/recognize` y `POST /api/register`. `POST /api/bulk_register` a propósito NO tiene este segundo gate (cargar el CSV de invitados es preparación previa al evento). **Bloqueo real, no solo aviso (2026-09-19):** en `kiosk_registro.html`, el botón "Escanear Rostro" y todo el `<form id="regForm">` (envuelto en un `<fieldset disabled>`) quedan deshabilitados de verdad cuando `event.status != "en_proceso"` — antes solo había un aviso visual y el usuario podía llenar el formulario igual para enterarse del error hasta darle guardar. La carga masiva (`kiosk_roster.html`) queda siempre habilitada a propósito, mismo criterio que el backend (`POST /api/bulk_register` no tiene el gate de `en_proceso`).
  - **Códigos únicos (2026-09-19, migración `0007`):** `Event.event_code` es único en toda la tabla (no solo dentro de un tenant) — `create_event`/`update_event` devuelven 400 con mensaje claro si ya existe, antes de tocar la base. `Tenant.client_code` es un campo nuevo, generado al azar (`secrets.token_hex(4).upper()`, reintentando si choca) al crear un tenant — pensado como identificador corto para referenciar/asociar un cliente sin exponer su `id` (que sigue siendo el slug del nombre). Se muestra junto al nombre del cliente en el dashboard.
  - **Validaciones de fechas (2026-09-19, `_validate_event_dates` en `routers/events.py`):** `start_date` no puede ser posterior a `end_date`; `setup_date` debe ser el día de `start_date` o antes. Se corre tanto al crear (los tres campos siempre están presentes) como al editar (se calculan los valores "efectivos" combinando lo que llega en el PATCH con lo que ya tenía el evento, antes de validar y antes de aplicar los cambios).
- `EventStaffAuthorization(event_id, staff_user_id, authorized_by_id)` — qué cuenta de staff puede operar en qué evento. **Ya se aplica en la práctica**: `app/auth.get_event_for_staff()` la exige para `digitador` (403 si no está autorizado, o si el evento ya está `cerrado`) en `/kiosk/{event_id}` y en cada endpoint de `/api` que recibe `event_id`. `coordinador`/`admin`/`super_admin` no la necesitan (alcance global sobre todos los tenants/eventos).

### Roles y permisos (`app/auth.py`)
`STAFF_ROLES` (`models.py`) = `("cliente", "digitador", "coordinador", "admin", "super_admin")`. `require_role("x")` exige ese nivel de jerarquía o superior — **pero `cliente` NO es realmente parte de esta jerarquía**, solo ocupa el nivel más bajo numéricamente para que `require_role("digitador")` lo excluya automáticamente de recognize/register (que es lo único que importa: un cliente jamás debe poder registrar/reconocer). Para todo lo que SÍ necesita ver (directorio, su propia lista de eventos) los endpoints usan `get_current_staff` en vez de `require_role`, precisamente para no quedar atados a esa jerarquía — ver `/api/users` y `/api/my-events` en `routers/api.py` y `routers/events.py`.

| Acción | Requisito |
|---|---|
| Reconocer / registrar persona en SU evento autorizado (`/api/recognize`, `/api/register`) | rol `digitador` o superior |
| Ver directorio en vivo de un evento (`GET /api/users`), ver "mis eventos" (`GET /api/my-events`) | cualquier staff autenticado con autorización a ese evento (incluye `cliente` y `digitador`) |
| Editar perfil, carga masiva, descargar reporte, crear/editar cliente y evento, crear/quitar usuarios temporales (digitador) de un evento | rol `coordinador` o superior |
| Borrado permanente (logs de un usuario, eliminar evento/cliente), crear/desactivar cuentas `coordinador`/`cliente`, reautorizar un digitador o cliente existente para otro evento | rol `admin` o superior |
| Eliminar un asistente de un evento (`DELETE /api/users/{id}`) y cambiar su estado registrado/no registrado (`PATCH .../status`) — **ampliado a coordinador+ el 2026-09-17 (Fase 11), pedido explícito**; antes admin+ solamente, ver esa fase arriba | rol `coordinador` o superior |
| Crear cuentas `admin` | `super_admin` (único caso que no es "jerarquía", hardcodeado en `routers/staff.py`) |

**Fix (2026-09-16): un Super Admin podía desactivarse a sí mismo** (o a otro Super Admin) desde `/admin/staff` — como esa tabla se muestra sin filtrar para `super_admin`, se ve a sí mismo en la lista, y `PATCH /staff/{id}/deactivate` no tenía ningún chequeo de auto-desactivación ni de "nunca desactivar un super_admin". Corregido: un `super_admin` nunca puede desactivarse vía ese endpoint, y nadie puede desactivar su propia cuenta. La UI (`staff.html`) también oculta los botones de Desactivar/Borrar en la propia fila y en cualquier fila `super_admin`. Si esto ya te pasó y quedaste sin poder entrar, se arregla directo en la base (no hay otra forma, por definición no puedes loguearte para usar el botón de reactivar): `UPDATE staff_users SET is_active = true WHERE username = '<tu_usuario>';`.

**Borrado permanente de cuentas de staff (2026-09-16):** `DELETE /api/staff/{id}` (distinto de `/deactivate`, que solo desactiva) — cada rol solo borra lo que tiene directamente debajo, tabla `DELETABLE_ROLES_BY` en `routers/staff.py`: `coordinador` → solo `digitador`; `admin` → `coordinador`/`digitador`/`cliente`; `super_admin` → `admin`/`coordinador`/`digitador`/`cliente`. Nadie puede borrarse a sí mismo. No borra en cascada `Event`/`AccessLog` (son datos de negocio reales) — las referencias a la cuenta borrada quedan en `NULL`, solo se borran las filas de `EventStaffAuthorization` propias (esas sí pierden sentido sin la cuenta). UI: botón "Borrar permanente" en `/admin/staff` y en la pestaña "Usuarios Temporales" del kiosko.

**Usuario `cliente` (2026-09-16, ajustado 2026-09-17):** al igual que `digitador`, se crea y se autoriza **desde dentro de un evento** (pestaña "Usuarios del Evento" en `/kiosk/{event_id}`, `POST /api/events/{id}/staff-users` con `role=cliente`) — NO desde `/admin/staff` (ese formulario solo crea `coordinador`, y `super_admin` además `admin`). La diferencia con `digitador`: crear una cuenta `cliente` exige `admin`+ (un `coordinador` solo puede crear `digitador` ahí mismo). Asignar una cuenta `cliente`/`digitador` YA EXISTENTE a otro evento también se hace desde esa misma pestaña ("Asignar cuenta ya existente", `admin`+, `POST .../staff-users/assign-existing`) — el viejo `POST/DELETE /staff/{id}/authorize-event/{id}` en `routers/staff.py` se quitó, quedó reemplazado por esto. Al loguearse con exactamente un evento activo autorizado, `cliente` entra derecho a `/kiosk/{event_id}` igual que `digitador`, pero ahí solo ve dos pestañas: **Estadísticas** (placeholder, todavía sin diseñar/mockeado) y **Directorio en Vivo**.

**Usuarios temporales (digitador) — flujo real (2026-09-16):** ya NO se crean desde `/admin/staff` (esa opción se quitó del formulario a propósito). Se crean desde dentro del evento (`/kiosk/{event_id}`, pestaña "Usuarios Temporales", visible para `coordinador`+), vía `POST /api/events/{event_id}/temp-users` — crea el `StaffUser` y la `EventStaffAuthorization` en la misma transacción, así que quedan asociados al evento desde el primer momento, nunca "sueltos". `/admin/staff` conserva un formulario aparte para **reautorizar** (no crear) un digitador ya existente en un evento distinto — eso sigue siendo `admin`+.

### Multi-tenant: ahora sí explotado
Ya no hay `CURRENT_TENANT` hardcodeado en ningún router. El tenant de cada operación se resuelve siempre a partir del `Event` (`event.tenant_id`), y los tenants se crean/administran de verdad desde `/` (coordinador+) vía `/api/tenants`.

### Motor biométrico (`app/biometrics.py`)
- `num_jitters=25` en registro inicial, `10` en reconocimiento — más remuestreo al registrar para un encoding más estable.
- Tolerancia de comparación ajustada a **0.55** (default de la librería es 0.6, la bajaron a propósito para ser más estrictos y reducir falsos positivos).
- Sin control de calidad de imagen (blur, múltiples caras, spoofing) antes de generar el encoding.

## Convenciones del proyecto
- Español para nombres de negocio (mensajes de API, comentarios de dominio); código e identificadores técnicos en inglés/estándar.
- Sin capa de servicios: los routers hablan directo con SQLAlchemy y con `BiometricEngine`. Está bien para el tamaño actual; si crece, separar en servicios antes de que `api.py` se vuelva inmanejable.
- Los endpoints devuelven dicts planos (`{"result": ..., "data": ...}`), no hay schemas Pydantic de respuesta.

## Seguridad — incidente resuelto (2026-09-14)
Se encontró y corrigió una exposición de credenciales real en producción:
1. **`static/.env` estaba commiteado al repo público** con la cadena de conexión real de PostgreSQL. Como `static/` se monta directo como `StaticFiles` en `/static` (`app/main.py:13`), ese archivo era descargable por HTTP en producción (`/static/.env` devolvía 200 con la credencial en texto plano — confirmado y luego verificado como cerrado). → Se movió a `.env` en la raíz (gitignored) y se quitó del índice de git.
2. **`app/database.py` tenía esa misma credencial quemada como valor por defecto** si `DATABASE_URL` no estaba seteada. → Ahora la app falla al arrancar (`RuntimeError`) si no hay `DATABASE_URL` en el entorno, en vez de caer silenciosamente a una credencial real.
3. Se agregó `.env.example` con placeholders y se reforzó `.gitignore` (`.env`, `static/.env`, `data/`, `known_people/`, `__pycache__/`).
4. **La contraseña de PostgreSQL en producción fue rotada** y el `.env` del servidor actualizado con la nueva.
5. **El historial de git fue reescrito** (`git filter-repo`, force-push a `main`) para eliminar tanto el archivo `static/.env` como cualquier commit anterior de `app/database.py` que contuviera la credencial vieja como texto plano. Si tienes un clon local anterior a esta fecha, su historial ya no coincide con `origin/main` — no hagas `git pull` normal sobre él, hay que resincronizar (`git fetch origin && git reset --hard origin/main`, perdiendo cualquier commit local no pusheado) o volver a clonar.

## Qué falta (backlog real, no aspiracional)
- ~~Instalar Tesseract OCR en la VM de producción~~ **Resuelto (2026-09-16, Sprint 2.3):** `deploy.yml` ahora instala `tesseract-ocr` solo (`sudo apt-get install -y tesseract-ocr`) en cada deploy — ya no depende de un paso manual. Se confirma en el primer deploy real a producción (no se puede probar en este sandbox, no hay VM).
- **Parser MRZ validado con una sola muestra real.** El brief lo marca explícitamente: antes de darlo por definitivo conviene confirmarlo con 2-3 cédulas nuevas más (personas distintas) para asegurar que el largo/posición de cada campo no cambia entre documentos — la librería `mrz` ya valida el checksum de cada lectura individual, pero eso no reemplaza probar con más muestras reales.
- ~~Sin autenticación~~ **Resuelto (2026-09-14):** login + 4 roles. ~~Sin conectar EventStaffAuthorization~~ **Resuelto (2026-09-15):** ver `get_event_for_staff` y sección Navegación. Sigue faltando: CSRF explícito (mitigado parcial por `SameSite=Lax`), rate limiting de intentos de login, y una UI de "olvidé mi contraseña" (hoy solo un admin puede resetear, manualmente).
- **Sin tests.** No hay carpeta `tests/` ni configuración de pytest.
- ~~Sin migraciones~~ **Resuelto (2026-09-14):** Alembic (ver sección Migraciones abajo). `Base.metadata.create_all` ya no se llama desde `main.py`.
- ~~Sin CI/CD~~ **Resuelto (2026-09-14):** `.github/workflows/deploy.yml` corre en un runner self-hosted instalado directo en la VM (`golden-biometrics-prod`). Se eligió self-hosted y no SSH-desde-GitHub porque el proyecto tiene **OS Login activado** en GCP, que bloquea el acceso SSH por llave externa — el runner evita ese problema porque corre dentro de la VM, no entra desde afuera. En cada push a `main`: instala deps, `alembic upgrade head`, `systemctl restart facial-recognition`. También soporta disparo manual (`workflow_dispatch`).
  - **Nota de seguridad:** el repo es público. El workflow solo se dispara con `push` a `main` (requiere permiso de escritura al repo), nunca con `pull_request`, así que un PR externo no puede ejecutar código en el runner de producción. Si en algún momento se agrega un trigger de `pull_request` o `pull_request_target`, hay que exigir aprobación manual para colaboradores externos.
- ~~Multi-tenant no explotado~~ **Resuelto (2026-09-15)** (ver arriba).
- **`bulk_register` no valida CSV/ZIP de forma robusta**: `except: pass` silencioso al procesar imágenes del zip (`api.py`), puede ocultar errores reales de registros que no se cargaron. Tampoco pide `event_id` desde la UI del formulario CSV todavía más allá de lo ya cableado en `app.js`.
- **README ausente** — no hay instrucciones de instalación/arranque para alguien nuevo en el proyecto.
- **`requirements.txt` sin versiones fijadas** — riesgo de que una actualización de `face_recognition`/`dlib` rompa el build en un entorno nuevo.
- **Sin roles/permisos granulares por usuario individual** — 4 roles fijos en código a propósito (más simple; no hay todavía un catálogo real de "acciones" del sistema grande para armar un checklist granular).
- **Sin edición/eliminación de `StaffUser` más allá de activar/desactivar** — no hay endpoint para cambiar contraseña de otra cuenta o editar su rol una vez creada; hay que desactivarla y crear una nueva si algo queda mal.
- **Pendiente (siguiente push en esta misma rama): archivos adjuntos por evento, cifrados en la base de datos.** Pedido explícito del usuario — subir varios archivos (xlsx/pdf/docx/txt/imágenes) asociados a un evento, guardados cifrados, descargables después por cualquier usuario con acceso al evento. Todavía no implementado. Decisión pendiente de documentar cuando se haga: esquema de cifrado (probable: `cryptography.Fernet` con una `FILE_ENCRYPTION_KEY` nueva en `.env`, cifrado simétrico servidor-side — no hay pedido de cifrado end-to-end donde el servidor no pueda leer el archivo).
- ~~Selector de hora tipo reloj~~ **Resuelto (2026-09-17), bug de selección arreglado el mismo día:** `static/js/clockpicker.js`, widget custom (SVG, sin dependencias externas) — reemplaza el `<input type="time">` nativo en los 4 campos de hora del formulario de evento. `attachClockPicker(input)` sobre un `<input type="text" readonly>`. **Bug real que tuvo:** al hacer clic en un número, `renderFace()` reconstruye el `<svg>` (`innerHTML = ""` + nuevos nodos), lo que **desprende del DOM** al `<circle>` que originó el clic; cuando ese click sigue burbujeando hasta el listener de "cerrar si el clic fue afuera" (en `document`), `popupEl.contains(e.target)` da `false` porque el nodo ya no está en el árbol — y el popup se cierra solo, sin registrar la hora. Se arregló con `e.stopPropagation()` en los círculos (y en el contenedor del popup, como refuerzo). **Lección para cualquier otro widget con popup + reconstrucción de DOM en este proyecto: nunca mutar el subárbol que contiene `e.target` antes de que el evento termine de burbujear — cortar la propagación primero.**
- ~~Ciudades por país~~ **Resuelto de verdad (2026-09-17):** ya no es una lista escrita a mano — `app/cities_by_country.json` (~20,300 ciudades) se generó una sola vez offline desde el dataset público de GeoNames (`cities15000.txt`, CC BY 4.0, ciudades con población ≥15,000 en todo el mundo), filtrado a los ~36 países que soporta el formulario y ordenado por población. `app/cities_data.py` solo carga ese JSON al arrancar; `GET /api/cities?country=X` (en `routers/events.py`) devuelve las primeras 300 (para no mandar un `<datalist>` gigante al navegador en países con miles de ciudades) — Ciudad sigue aceptando texto libre para lo que quede fuera de esas 300. Si se necesita regenerar o ampliar el dataset (otro corte de población, más países), el proceso fue: descargar `cities15000.zip` y `countryInfo.txt` de `download.geonames.org/export/dump/`, y correr un script Python que mapea ISO2→nombre en español y arma el JSON — no quedó versionado el script en el repo, solo el resultado.
- **Corregido (2026-09-16): el formulario de "Nuevo evento" en el dashboard caía a un envío nativo del navegador (recarga con los datos en la URL) en vez de pasar por `fetch`.** La causa más probable era depender de atributos `onsubmit`/`onclick` inline sobre HTML inyectado dinámicamente vía `innerHTML`. Se migró todo `templates/dashboard.html` a **delegación de eventos** (un único listener en el contenedor estable `#tenantsList`, usando atributos `data-action`/`data-*` en vez de `onsubmit`/`onclick` inline) — es el patrón a seguir para cualquier HTML generado dinámicamente en este proyecto de ahora en adelante, no volver a usar `onsubmit="fn(...)"` inline sobre contenido inyectado por JS.
- **Backup solo local** (`~/backups` en la VM, cron diario, rotación 7 días vía `pg_dump`). Backup offsite (bucket GCS) no está armado.
- **Monorepo:** el repo hoy sigue siendo solo este módulo en la raíz. Ya se decidió la estrategia (monorepo, `apps/<módulo>/`) pero la reestructuración física todavía no se ejecutó — es la próxima rama.

## Migraciones (Alembic)
El esquema ya no se crea con `Base.metadata.create_all` — vive como migraciones versionadas en `alembic/versions/`.
```bash
alembic upgrade head          # aplica todas las migraciones pendientes
alembic revision -m "mensaje" # crea una migración nueva vacía (escribirla a mano, no autogenerate: no hay DB de referencia limpia garantizada)
```
**Importante para la VM de producción y cualquier DB que ya tenía las tablas `tenants`/`users`/`access_logs` de antes de que existiera Alembic** (creadas por el viejo `create_all`): la primera vez, en vez de `alembic upgrade head` derecho, hay que marcar la migración baseline como ya aplicada sin ejecutarla (`alembic stamp 0001_baseline`) y de ahí sí `alembic upgrade head` (que solo corre `0002_auth_roles_events` en adelante). Una DB nueva/vacía sí corre `upgrade head` normal desde el principio.

**Bug real encontrado por Juan David (2026-09-17, corrido contra Postgres real por primera vez desde que empezó Sprint 2.4)**: `alembic upgrade head` truncó con `StringDataRightTruncation` al llegar a la migración `0023` — el `revision id` que se le había puesto (`0023_access_log_registration_method`, 35 caracteres) no cabía en la columna `alembic_version.version_num`, que Alembic crea sola como `VARCHAR(32)`. Como Postgres corre cada migración en una sola transacción (DDL incluido), el fallo del `UPDATE` final revirtió TODO ese paso — no quedó ninguna migración a medias, solo hubo que acortar el id a `0023_access_log_reg_method` (26 caracteres) y correr `alembic upgrade head` de nuevo. **Cualquier `revision id` nuevo debe quedar en 32 caracteres o menos** — no había pasado antes porque todo el testing de este sprint corrió sobre SQLite sembrado directo con `Base.metadata.create_all()` (ver más abajo), nunca sobre el flujo real de `alembic upgrade head` migración-por-migración hasta que Juan David lo corrió contra Postgres.

Para crear la primera cuenta (Super Admin, no hay UI para esto por diseño — sería circular): `python scripts/create_staff_user.py --username <tu_usuario> --role super_admin --full-name "..."`.

## Flujo de git (a partir de 2026-09-14)
- Toda rama que no sea `main` se puede empujar libremente — `deploy.yml` solo dispara con push a `main`, así que una rama nunca toca producción sola.
- Cambios de código/esquema: rama nueva → implementar → avisar al dueño del repo → él prueba en su entorno local (tiene Postgres + `pgAdmin4` con una copia de `golden_db`) → si aprueba, merge a `main` → el runner self-hosted despliega solo.
- No mergear a `main` sin autorización explícita, aunque el cambio "se vea listo".

## Cómo correrlo localmente
```bash
pip install -r requirements.txt
cp .env.example .env   # y editar DATABASE_URL (ENVIRONMENT=development deja /docs abierto)
alembic upgrade head   # crea/actualiza el esquema (ver sección Migraciones si la DB ya existía desde antes de Alembic)
python scripts/create_staff_user.py --username admin --role super_admin   # si no tienes cuenta todavía
uvicorn app.main:app --reload --port 5000
```
Requiere PostgreSQL corriendo y accesible con la URL de `.env`. `face_recognition` depende de `dlib`, que en Windows suele requerir Visual C++ Build Tools o usar un wheel precompilado. El módulo de lectura de cédula por foto (Sprint 2 Parte 2, `app/mrz_ocr.py`) necesita además el binario de **Tesseract OCR** instalado en el sistema — sin él, el resto de la app funciona normal, solo ese endpoint puntual responde 503 (ver sección "Lector de cédula").

## Infraestructura (producción)
- VM Google Cloud (`golden-biometrics-prod`, Ubuntu), IP pública estática, **OS Login activado** (bloquea SSH por llave externa/metadata — por eso el runner de CI/CD es self-hosted y no SSH-desde-afuera).
- Nginx como reverse proxy + terminación TLS (certificados Certbot). `sites-enabled/golden` — un solo server block, nada raro servido fuera de la app.
- Cloudflare delante como proxy/DNS.
- Postgres escucha solo en `localhost` (confirmado), `pg_hba.conf` exige `scram-sha-256` incluso en loopback — no hace falta endurecer nada ahí. `ufw` inactivo (normal en GCP, el firewall real es a nivel de red del proyecto, no se tocó).
- **La app se conecta con el rol `golden_app`** (mínimo privilegio sobre `golden_db`), no con el superusuario `postgres` (resuelto 2026-09-14). El rol `postgres` queda solo para administración manual.
- Backup diario automático (`pg_dump` + cron a las 3am, rotación local de 7 días en `~/backups`).
- Systemd (`facial-recognition.service`) mantiene viva la app FastAPI; se reinicia solo si cae.
- Deploy automático vía runner self-hosted de GitHub Actions instalado en la propia VM (ver `.github/workflows/deploy.yml`): en cada push a `main` instala dependencias, corre `alembic upgrade head`, y reinicia el servicio.
- Sigue faltando: `Dockerfile` y los unit files de systemd versionados en el repo (hoy `facial-recognition.service` solo existe en `/etc/systemd/system/` de la VM, no en git); backup offsite.
